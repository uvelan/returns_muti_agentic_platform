{
  "export_command": "poetry run python -c \"import json; from return_platform.main import create_app; print(json.dumps(create_app().openapi(), indent=2))\" > ../frontend/openapi/return-platform.openapi.json",
  "working_directory": "backend",
  "notes": "Generates the backend OpenAPI schema for use by openapi-typescript. To be automated in contracts:generate."
}
