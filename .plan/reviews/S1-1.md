# RV review — S1 (model & identity), round 1

- **Branch:** `feat/s1-model-identity` (final commit `7438e07`)
- **Base:** `a50c5500788f99e909f23099a81731b37c736b8c`
- **Diff reviewed:** `git diff a50c550..7438e07` — 8 files, +1136/-0 (purely additive)
- **Contract:** `.plan/contracts.md` §4 (+ §1 DR-11); brief `.plan/tracks/S1.brief.md`
- **Reviewer:** RV — Date: 2026-08-30

## Verdict: PASS

Zero unresolved findings. The diff was reviewed directly; ledger claims were independently re-verified where they were load-bearing (see below).

## What was verified

**Contract conformance (§4 / DR-11)**

- `CaseFactView.record_scope: str | None = None` and `identity_version: int | None = None` — additive, defaulted; a pre-S1 stored document validates unchanged (tested against the exact pre-S1 key set under `extra="forbid"`). Field spellings match the contract exactly.
- `FactAcquisition.ASSOCIATE_EDIT` / `CONTEXT_SUMMARY` added; original four members untouched (tested).
- `append_scoped_fact_once` is a new sibling in `workflows/return_case_activities.py`; `_append_fact_once` and `append_case_fact` are byte-untouched. Identity includes `record_scope` (`{fact_id}::{record_scope}`); scope-`None` derives the exact legacy id — the required replay test (legacy pre-deploy write, retry through the new path → absorbed, legacy document shape untouched) is present and passes.
- `latest_case_facts_scoped` partitions by `(record_scope, factName)` for a case, same `(recordedAt, factId)` tie-break as the legacy projection (compared against base source directly). `latest_case_facts` and its 11 consumers unmodified.
- `fact_names.py`: exactly two constants (`support_artifact_ambiguous`, `support_artifact_unmatched`), both referenced by `artifact_binding.py` — no unused constants (contract §4 requirement honored, verified by grep).
- Binding rules in `artifact_binding.py` are code, exhaustive over the contract's table: known ref → BOUND; unknown ref → UNMATCHED + scoped fact, never a record (the persistence port has no create method at all — DR-11 enforced structurally); no ref + 1 record → BOUND; no ref + N → AMBIGUOUS + scoped fact. Statuses exactly `BOUND|AMBIGUOUS|UNMATCHED`. BOUND merges under `RETURN_RECORD_MERGED_FIELDS` semantics: only the artifact's field written, blank value writes nothing, redelivered value writes nothing, optimistic version CAS with one re-read retry then propagate. Every branch tested.
- Idempotency: AMBIGUOUS/UNMATCHED facts keyed `{fact_name}-{case_id}-{dedupe_key}` through the append-once path (stable key, check-then-act via unique `factId`); BOUND merge is a value-equality no-op on redelivery. Provenance on every fact write: agent, channel, method, timestamps (provenance literals follow the established base-code callsite pattern, e.g. `api/cases.py:1109`).
- Ownership: all 8 files are S1's declared set + ledger. No shared files, no workflow-definition file, no API, no frontend.

**Standing greps (this round)**

- Fact-name literals outside `operations/fact_names.py`: none — the two names appear as literals only in `fact_names.py` itself; all other references import the constants.
- New imports of frozen modules (`operations/associate_flow`, `agents/order_discovery`, `api/associate_returns`, `api/return_agents`): none.
- Template/section/intent/tool literals: none (no template or routing surface in this slice).

**Independent test verification (not taken from the ledger)**

- The 30 new tests run green at `7438e07` (run by RV in a detached worktree).
- Full default-deselection suite run by RV at both `7438e07` and base `a50c550` in the same detached-worktree harness: the failure/error sets are **byte-identical** (`diff` of sorted FAILED/ERROR lists → no difference; 4253 passed / 18 failed / 55 errors on each side). RV's harness surfaces more environmental failures than the author's in-place run did (live-infra-adjacent policy/graph/config suites erroring identically at base — harness artifacts, not code), but the discriminating fact holds either way: **the S1 diff introduces zero failures that base does not already have**, and adds 30 passing tests.
- Both claimed pre-existing failures confirmed individually by RV at base `a50c550`: `test_cumulative_support_outcomes.py::test_a_rejected_return_still_opens_no_work_item` and `test_main_is_composition_only.py::test_router_mounting_is_the_bulk_of_create_app_and_that_is_allowed` fail identically on the untouched base. Not S1 findings; they remain charged to the environment/base, flagged to the orchestrator.

## The three ledger-recorded interpretations — judged on the diff

1. **Placement in `case_repository.py`** — ACCEPTED. The brief's anchor is "existing `latest_case_facts`", which is defined on `CaseRepository` in `case_repository.py:388` (verified at base); `operations/repository.py` defines no fact methods and exposes them by inheritance (`OperationalRepository(CaseRepository)`). Placing the scoped siblings beside the real definitions follows the brief's own "follow where the anchor lives" rule; they reach `OperationalRepository` exactly as the legacy methods do. No scope widened.
2. **No reference + zero records → UNMATCHED** — ACCEPTED. The contract's table does not cover the case; UNMATCHED is the only status consistent with its invariants (nothing to bind, nothing to disambiguate between, record creation forbidden), and it lands in the map-or-reject clarification flow rather than guessing. Documented in module and test.
3. **`ARTIFACT_STORED_FIELDS` mirrors `RETURN_RECORD_MERGED_FIELDS` keys, pinned by a drift test** — ACCEPTED. Importing would point operations→workflows against the dependency direction (the workflows layer already imports operations, and V2 will consume this module from the operations side); the mirror is not config-destined vocabulary but a reflection of an existing code-side constant, and `TestStoredFieldMirror` fails on any stored-key rename or addition on the mirror side (`mirrored <= merged_stored_keys`). This is not the calibration pattern: the keys remain defined in exactly one authoritative place and the copy is machine-pinned to it.

## Advisories (non-blocking, no action required this round — recorded for later rounds)

- **A1.** Scoped facts land in the same `case_facts` collection, so a scoped write whose `factName` collides with a legacy name *will* surface in `latest_case_facts` (newest-wins per name) and could shadow a case-level value for the 11 frozen consumers. Nothing in this diff does that — S1's fact names are new and constant-guarded — but the guard is naming discipline, not code. RV will watch S2/V2 rounds for any scoped write of a legacy fact name. Relatedly, `test_legacy_projection_is_untouched_by_scoped_writes` asserts key-set shape only; its docstring's "exactly the mapping they always did" is broader than the assertion. Shape is what the brief freezes, so this stands as written.
- **A2.** The `::` identity separator assumes no legacy `fact_id` ends in `::<returnRecordId>`; the ledger records that none does today and `identity_version` makes the derivation auditable. Noted, agreed, no action.

## Findings

None.
