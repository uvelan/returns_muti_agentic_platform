# Merge state — orchestrator record

Base: `a50c5500788f99e909f23099a81731b37c736b8c` (`refactor/unified-return-platform`).
Merge order: `T0 → S1 → S2 → V1 → V2 → V3 → ACC`, RV `PASS` (zero unresolved findings) required between every arrow.
Each slice branches from the latest RV-approved integration commit recorded here; the integration agent's shared-file changes ride the same slice branch and are reviewed in the combined diff.

| Slice | Branch | Status | RV rounds | Merged at | Notes |
|---|---|---|---|---|---|
| T0 | (trunk) | IN_PROGRESS | — | — | investigations complete (contracts.md §2); DR-11 ruled; calibration fixture pending |
| S1 | feat/s1-model-identity | MERGED | 1 (PASS, zero findings) | 5d58b90 | review 6bdb5bd; S2/V1 pipelined bases confirmed valid (7438e07 = approved head); advisories A1 (legacy-name shadowing) + A2 (:: separator) watchlist for S2/V2 |
| S2 | feat/s2-delivery-spine | IN_PROGRESS (steps 01–03 done @1cf2d44; resumed at step:04) | — | — | PIPELINED off S1 approved head 7438e07. Agent killed by API session limit after step:03, resumed cold via ledger. Recorded deviations for RV: (a) brief path typo `operations/return_case_recovery.py` → real `workflows/return_case_recovery.py`; (b) append-only add of 2 constants to S1's fact_names.py (that file's own docstring invites it; contract forbids literals elsewhere) |
| V1 phase 1 | feat/v1-template-review | UNDER_RV_REVIEW (candidate d452e97, off 7438e07) | 1 open | — | Items 1,2,5 complete: template grammar + formatter allowlist + production.yaml default (char-for-char equivalent to compose_support_handoff, subject-grammar caveat recorded), renderer (batched sync, gaps, within-draft cache), preview endpoint, config UI on the literally-reused DocumentEditor (extracted to its own file; AgentsSection's 32 tests untouched as the preservation proof). +21 frontend tests, 0 introduced failures. **Brief error found by the slice:** brief said governance-proposal write path; that endpoint refuses non-agent modules, so publishing rides the release lifecycle per contracts §8 — the contract was right, my brief was wrong. Also closed 3 stale OpenAPI snapshots that were already red, and enveloped a preview response step:04 shipped bare. Phase 2 (gate/API/panel UI) dispatches after S2 merges |
| ACC-1 | feat/acc-harness | CHANGES_REQUIRED round 1 → fixing | 1 (CHANGES_REQUIRED, review ba19fd8) | — | **RV found 2 rule-10 holes by fault injection** (mutated code to prove the tests catch drift; they didn't): F1 the calendar pin's holiday claim is unasserted (empty→empty maps identically, so a dropped holiday set stays green); F2 stop-vs-kill "pin" only asserts the process stopped, so deleting stop()'s graceful block entirely left 22 tests green. Guard + chaos primitives independently re-proved sound. Advisory carried to phase 2: non-`default` calendar id alone doesn't make a *forgotten* fixture loud — scenarios must assert calendar_applied/not is_continuous. Original candidate f433237, off e0a5f6c | Phase 1 (brief items 1,2,7) complete: fact-name AST guard reading vocabulary from fact_names.py at runtime + proved against a literal-bearing source; Mon–Fri calendar fixture (id deliberately ≠ `default` so a forgetting scenario fails loudly); chaos_restart primitives. +35 passing, 0 new failures, 0 production files touched. Residual risk: live smoke test never executed (datastores down) — flagged for the acceptance run. Author-reported harness bug found+fixed by its own tests (orphaned children); job-object alternative measured worse and removed. Open observation for a future slice: promoting the private config→domain calendar mapping onto operations/business_calendar.py would let ACC drop a deliberate duplicate |
| V2 | feat/v2-ingress-relay | NOT_STARTED | — | — | |
| V3 | feat/v3-resolver-clarification | NOT_STARTED | — | — | |
| ACC | feat/acc-acceptance | NOT_STARTED | — | — | |
| RV calibration | rv-calibration/seeded-hardcoding | CAUGHT (CHANGES_REQUIRED, F1 blocking rule-1) | 1 | never merges | review at .plan/reviews/calibration-1.md (commit d59e017); RV cleared to gate real branches |
