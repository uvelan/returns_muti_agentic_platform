# Merge state — orchestrator record

Base: `a50c5500788f99e909f23099a81731b37c736b8c` (`refactor/unified-return-platform`).
Merge order: `T0 → S1 → S2 → V1 → V2 → V3 → ACC`, RV `PASS` (zero unresolved findings) required between every arrow.
Each slice branches from the latest RV-approved integration commit recorded here; the integration agent's shared-file changes ride the same slice branch and are reviewed in the combined diff.

| Slice | Branch | Status | RV rounds | Merged at | Notes |
|---|---|---|---|---|---|
| T0 | (trunk) | IN_PROGRESS | — | — | investigations complete (contracts.md §2); DR-11 ruled; calibration fixture pending |
| S1 | feat/s1-model-identity | MERGED | 1 (PASS, zero findings) | 5d58b90 | review 6bdb5bd; S2/V1 pipelined bases confirmed valid (7438e07 = approved head); advisories A1 (legacy-name shadowing) + A2 (:: separator) watchlist for S2/V2 |
| S2 | feat/s2-delivery-spine | UNDER_RV_REVIEW (candidate 4359bf9, off 7438e07) | 1 open | — | All 8 steps done. 4506 passed / 2 failed (known pre-existing pair), 0 introduced. Resume applied repo-beats-ledger correctly: found an uncommitted 1033-line review_aggregate.py, read it fully, caught a real bug (`self._database[planned.command_document and "case_command_records"]` — truthy by accident, `"integration_outbox"` hard-coded beside it), relocated the pair-write into case_commands.insert_planned() rather than rewriting the step. **Deviations:** (a) brief path typo operations/→workflows/return_case_recovery.py; (b) append-only fact_names.py additions; (c) NEW `CaseStatus.AWAITING_TEMPLATE_REVIEW` mapped onto existing ReturnCaseStatus.AWAITING_SUPPORT (read_persisted_status refuses unknown statuses, so recovery would raise on the first parked case) + forced OpenAPI regen. **Open risks flagged to RV:** AWAITING_SUPPORT now in LEGITIMATE_WAIT_STATUSES = behaviour change to an existing status (time-based sweep no longer relaunches null-workflowId cases; probe-based path still does) — RV must confirm the old sweep wasn't load-bearing; command-horizon rule unarmed until V1 wires it (degrades honestly); _wordpiece_approx_v1 is a versioned estimate, not a tokenizer. Design note: assemble_case_context types against a structural ContextPolicy protocol because platform/* may not import configuration/* (layering test 13.1 R2a) |
| V1 phase 1 | feat/v1-template-review | CHANGES_REQUIRED round 1 → fixing | 1 (CHANGES_REQUIRED, review f8ce598) | — | **F1 blocking:** default variant does NOT reproduce today's handoff — the green equivalence test's fixture makes every conditional take the one expressible branch; RV rendered shipped yaml through shipped renderer on a case with no associate + no bay recommendation and found 5 silent divergences (customer phone/email — a loss support_handoff.py documents as a defect it FIXED; Awaiting From Support incl. its UNKNOWN arm; Unresolved Reason; manual-bay action line; return_details.additional), all masked by fallbacks, zero gaps. Phase 2 swaps the composed path behind workflow.patched, so this would be a silent production regression. **F2 blocking:** 5 `case_fact:` bindings name facts that don't exist anywhere in backend/src; assembler seam is docstring-only; template_preview.py holds a second uncoordinated copy of the 24-key vocabulary. **F4 blocking:** `return_record:` resolves via unconstrained getattr → AMENDMENT-2 closes it. **F3 routed to orchestrator → AMENDMENT-1** admits `literal:` as a 4th binding source (frozen enumeration amended, not implementation excused). Cleared without finding: publish path (contract right, brief wrong), DocumentEditor extraction (behaviour-preserving line-by-line), OpenAPI/envelope (in-slice), rule 12, test integrity. Candidate was d452e97, off 7438e07 | Items 1,2,5 complete: template grammar + formatter allowlist + production.yaml default (char-for-char equivalent to compose_support_handoff, subject-grammar caveat recorded), renderer (batched sync, gaps, within-draft cache), preview endpoint, config UI on the literally-reused DocumentEditor (extracted to its own file; AgentsSection's 32 tests untouched as the preservation proof). +21 frontend tests, 0 introduced failures. **Brief error found by the slice:** brief said governance-proposal write path; that endpoint refuses non-agent modules, so publishing rides the release lifecycle per contracts §8 — the contract was right, my brief was wrong. Also closed 3 stale OpenAPI snapshots that were already red, and enveloped a preview response step:04 shipped bare. Phase 2 (gate/API/panel UI) dispatches after S2 merges |
| ACC-1 | feat/acc-harness | **MERGED** | 2 (r1 CHANGES_REQUIRED ba19fd8; r2 **PASS** 9cb3508) | see below | RV withdrew both findings only after re-injecting the faults itself (incl. a third probe of its own: `end_minute + 60`, also caught). Fact-name guard re-proved from scratch with RV's own planted violations. Zero production files touched across the branch. | Round 2: both findings fixed with before/after fault-injection evidence (F1 holiday drop: 10-passed miss → 1 failed; F2 stop/kill collapse: 22-passed miss → 1 failed). F1 pin now declares MONDAY a holiday so drift fails by a whole day — unmistakable, not a rounding artefact. F2 became three tests incl. an unrequested **source-level** pin (`force=False` in stop(), absent from kill()), reasoning that the behavioural pin is Windows-skipped and so could never object on this platform. +1 passed/+1 skipped vs reviewed commit, accounted for exactly. **Compounding residual risk for acceptance:** the behavioural SIGTERM test has never executed (Windows skip) AND the live smoke test has never run — both first execute on POSIX CI or during the acceptance run. Deferred by design: harness-level guard against a scenario that never calls with_business_calendar (its shape depends on V1–V3 surface; recorded as a phase-2 brief requirement instead). Round-1 candidate was f433237 | **RV found 2 rule-10 holes by fault injection** (mutated code to prove the tests catch drift; they didn't): F1 the calendar pin's holiday claim is unasserted (empty→empty maps identically, so a dropped holiday set stays green); F2 stop-vs-kill "pin" only asserts the process stopped, so deleting stop()'s graceful block entirely left 22 tests green. Guard + chaos primitives independently re-proved sound. Advisory carried to phase 2: non-`default` calendar id alone doesn't make a *forgotten* fixture loud — scenarios must assert calendar_applied/not is_continuous. Original candidate f433237, off e0a5f6c | Phase 1 (brief items 1,2,7) complete: fact-name AST guard reading vocabulary from fact_names.py at runtime + proved against a literal-bearing source; Mon–Fri calendar fixture (id deliberately ≠ `default` so a forgetting scenario fails loudly); chaos_restart primitives. +35 passing, 0 new failures, 0 production files touched. Residual risk: live smoke test never executed (datastores down) — flagged for the acceptance run. Author-reported harness bug found+fixed by its own tests (orphaned children); job-object alternative measured worse and removed. Open observation for a future slice: promoting the private config→domain calendar mapping onto operations/business_calendar.py would let ACC drop a deliberate duplicate |
| V2 | feat/v2-ingress-relay | NOT_STARTED | — | — | |
| V3 | feat/v3-resolver-clarification | NOT_STARTED | — | — | |
| ACC | feat/acc-acceptance | NOT_STARTED | — | — | |
| ACC-2 | (phase 2, not yet cut) | BLOCKED on V3 | — | — | Acceptance scenarios, brief items 3–6, 8–10. **Dispatch conditions inherited — see "Carry-forward conditions" below** |
| RV calibration | rv-calibration/seeded-hardcoding | CAUGHT (CHANGES_REQUIRED, F1 blocking rule-1) | 1 | never merges | review at .plan/reviews/calibration-1.md (commit d59e017); RV cleared to gate real branches |

## Carry-forward conditions (orchestrator-owned; must be written into the named future brief)

These were raised in review and assigned to the orchestrator, not to a slice. A slice cannot inherit them from a ledger note — they go into the brief at dispatch or they evaporate.

### Into the ACC phase-2 brief (RV, review ACC1-2)
1. **Business-time scenarios must assert, not assume.** The Mon-Fri fixture's id is deliberately not `default`, which prevents *shadowing* — but a scenario that simply never calls `with_business_calendar` silently inherits the shipped 24/7 dev calendar and runs on wall clock, green throughout, proving nothing. Every phase-2 scenario that depends on business time MUST assert `calendar_applied is True` or `not ...is_continuous`. Both instruments are proven and already exist. The harness-level guard was deliberately not built in phase 1 because its shape depends on V1-V3 surface that did not exist; decide at dispatch whether to build it then.
2. **Run the never-executed safety nets first.** Two of this harness's guarantees are arguments rather than observations: (a) `test_chaos_restart_smoke_real_infra.py` has never run - the datastores were down; (b) the behavioural SIGTERM pin is Windows-skipped, and RV narrowed the one unproven link to precisely `os.killpg(getpgid(pid), SIGTERM)` reaching the child through the session established at launch (everything upstream - spec construction, script generation, launch, stop, kill - was proven to execute on this platform). The smoke test must be the FIRST thing run when the stack comes up, ahead of anything built on it.

### Into the V1 phase-2 brief (S2 delivery-spine handover)
3. **Arm the command-horizon rule.** S2 shipped it unwired: until V1 passes `command_horizon=`, the recovery outcome honestly reports `RELAUNCHED` rather than pretending to have checked. V1 phase 2 must wire it.
