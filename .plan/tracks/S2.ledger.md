# S2 ledger — delivery spine + review aggregate + context assembly

Append-only. One entry per step (contracts.md §3).

---

## step:01 — branch + anchor verification

- **Branch:** `feat/s2-delivery-spine` created off `7438e07084c33b92971671cd821db8424b828778` (S1 candidate head, per coordinator instruction; supersedes the brief's "RV-approved commit in merge.md" because the run is pipelined — S1 is under RV in parallel).
- **Files touched:** `.plan/tracks/S2.ledger.md` (this file), `.env` (gitignored copy from main checkout root for the test harness — not committed).
- **Commands:** `git checkout -b feat/s2-delivery-spine 7438e07`; smoke test run `python -m pytest tests/operations/test_durable_support_events.py` → 40 passed.
- **Anchors verified:**
  - `operations/support_events.py` — `DurableSupportEventStore.record_support_response`: unique `(caseId, supportEventId)` (`SUPPORT_EVENT_IDENTITY_INDEX`), `canonical_payload_digest`, outbox insert in the same Mongo transaction, `DuplicateKeyError` → winner re-read. MATCH.
  - `operations/integrations/temporal_signal.py` — `TemporalSignalDispatcher` with lazy client factory, permanent/transient RPC classification, fixed signal name ("the topic is the signal"). MATCH.
  - `operations/integrations/outbox.py` — `IntegrationOutboxDispatcher.claim()` lease CAS, `CLAIMABLE_STATUSES`, `DEAD_LETTER_STATUS`/`REQUIRES_RECONCILIATION`, `ensure_integration_outbox_indexes` as the one index owner. MATCH.
  - `operations/return_support/service.py` — `ensure_indexes` at 427–453 (unique `(threadId, sequence)` at 450–452; unique partial `caseId` at 440–444), `open_case_thread` at 606–758 (unique on `caseId`, loser re-reads — the brief's "~667–753" window covers its body), `post_reminder` at 760+ (dedupe on `businessPayload.reminderKey` at 778). MATCH (line offsets shifted slightly from the brief's approximations; content exact).
  - `workers/integration_outbox.py` — topic→dispatcher registration map, reconciliation sweep task. MATCH.
  - S1 interfaces consumed (read-only): `operations/fact_names.py` (constants only, docstring invites later slices to append their own), `operations/case_repository.py::append_scoped_case_fact` + `latest_case_facts_scoped`, `workflows/return_case_activities.py::append_scoped_fact_once` (`{fact_id}::{record_scope}` identity, `identity_version` stamp), `operations/artifact_binding.py`. MATCH.
  - **Anchor discrepancy (recorded, not adapted-around silently):** brief item 7 names `operations/return_case_recovery.py`; the module exists uniquely at `workflows/return_case_recovery.py` and its content matches the brief's description exactly (`ReturnCaseRecoveryService`, relaunch gated on confirmed-absent/closed executions, dead-letter reconciliation sweep). Treated as a path typo in the brief, not a contract mismatch: contracts.md §2 investigation 4 and §3 both place recovery in `workflows/`. Proceeding against the real path; flagged in the delta report for RV.
- **Test result:** smoke green (40 passed).
- **Next step:** step:02 — `operations/case_commands.py` (DurableCaseCommandStore + signal dispatcher + tests).

---

## step:02 — per-case stream ordering on the outbox

- **Files touched:**
  - `backend/src/return_platform/operations/integrations/outbox.py` — `CaseStream` enum (`inbound|outbound|review_commands|omc`), `CASE_STREAM_SEQUENCES_COLLECTION` CAS counter, `allocate_case_stream_sequence`, `ordered_command_fields` (enqueue validation: `UnknownPredecessorError` for a predecessor that doesn't exist on the case, `PredecessorCycleError` for self-reference — with existence-required + immutable predecessor lists, self-reference is the only expressible cycle), ordering fields on `OutboxCommand` (defaults = legacy commands unchanged), two new named partial unique indexes (`case_stream_sequence_unique` on `(aggregateId, stream, streamSequence)`, `case_stream_event_id_unique` on `eventId`), claim-path gate in `dispatch_once` (`_ordering_hold` → defer without burning an attempt / park stream on unresolved dead-lettered predecessor, incl. earlier-in-stream query so late enqueues into a parked stream are caught), `PARKED_STREAM` status (not claimable), `logger.error("case_stream_parked")` as the ops alert.
  - `backend/src/return_platform/workflows/return_case_recovery.py` — `CaseStreamRecovery` (audited operator skip/retry, CAS on `orderingResolved`; skip writes fact `support_stream_skip` via injected `ScopedCaseFactRepositoryPort` with `record_scope=None` and S1's `SCOPED_FACT_IDENTITY_VERSION` resolved lazily; retry requeues with attemptCount kept; both unpark).
  - `backend/src/return_platform/operations/fact_names.py` — **append-only** addition of `SUPPORT_STREAM_SKIP` and `CONTEXT_SUMMARY` constants. Recorded deviation: the coordinator said "never edit" S1 modules; the file's own S1-written docstring says "Later slices append their own as they earn them", and the contract forbids fact-name string literals outside this file — so referencing these facts at all requires the append. Append-only, existing lines untouched. Flagged for RV.
  - `backend/tests/operations/mongo_double.py` (new shared double: dotted paths, `$ne`/`$gte`, upsert, `update_many`), `backend/tests/operations/test_case_stream_ordering.py` (new), `backend/tests/operations/test_integration_outbox_indexes.py` (EXPECTED_UNION extended with the two new declarations — the file's own stated maintenance path).
- **Commands:** pytest on the four touched/adjacent test files; ruff check+format.
- **Test result:** 93 passed (ordering 15, support events 40, index union 9, recovery reconciliation suite green).
- **Anchors verified:** `claim()` filter uses `CLAIMABLE_STATUSES` (`PARKED_STREAM` excluded by construction); `_dead_letter` semantics untouched.
- **Next step:** step:03 — `operations/case_commands.py`.

---

## step:03 — DurableCaseCommandStore + closed-map signal dispatcher

- **Files touched:**
  - `backend/src/return_platform/operations/case_commands.py` (new) — `CaseCommandKind` (7 review signals + `review_delivery_retry`/`review_abandon`), `SIGNAL_FOR_COMMAND_KIND` closed allowlist, `DurableCaseCommandStore` (`plan_command` for callers owning a larger transaction — the review aggregate's approval — and `record_command` owning its own; unique `(caseId, signalId)` = `case_command_signal_unique`; frozen CAS partial unique `(caseId, reviewId, draftVersion, canonicalEditVersion)` = `case_command_review_version_unique` → `StaleReviewVersionError` for the API's 409; same-signal digest compare → no-op or `CommandIdempotencyConflictError`; payload validation: review-scoped kinds must carry `review_id`+`scope_id`; `caseSequence` allocated on the `review_commands` stream via step:02's `ordered_command_fields` inside the same transaction), `CaseCommandSignalDispatcher` (TemporalSignalDispatcher shape, signal name from the closed kind map, forged kind → permanent dead-letter).
  - `backend/src/return_platform/operations/repository.py` — one call `ensure_case_command_indexes(self._db)` beside the support-events precedent (S2 owns schemas/indexes/repositories per §10 boundary).
  - `backend/src/return_platform/workers/integration_outbox.py` — dispatcher registration for `return-case.command.signal` (the one worker file the brief names).
  - `backend/tests/operations/test_case_command_store.py` (new, 15 tests), `mongo_double.py` (`$type: "number"` support).
- **Test result:** `tests/operations` — 654 passed, 126 deselected (live_infra/browser), 0 failed.
- **Anchors verified:** `record_support_response` transaction/duplicate-classification shape mirrored; `SUPPORT_EVENT_AGGREGATE_TYPE` reused for the operator listing.
- **Next step:** step:04 — `operations/review_aggregate.py`.

---

## step:04 — the review aggregate, whole

*(Resumed by a cold agent after the previous session was killed by an API session
limit. Repo verified against the ledger first: `git log` showed step:01 `c888fbe`,
step:02 `1a1b6c8`, step:03 `1cf2d44` off S1 head `7438e07`, exactly as recorded.
The one disagreement was an **uncommitted** `operations/review_aggregate.py` — the
killed step's work-in-progress. It was read in full, verified against contracts.md
sect. 6 and against step:03's actual `plan_command`/`classify_duplicate` surface,
found sound except for one garbled expression, repaired, and finished here rather
than rewritten. No completed step was redone.)*

- **Files touched:**
  - `backend/src/return_platform/operations/review_aggregate.py` (new) — `ReviewKind`
    (`TEMPLATE|SUPPORT_REPLY`), `ReviewState` + `_TRANSITIONS` (the frozen table,
    literally; `_guarded_update` asserts every transition it is asked to make is in
    it, so an illegal arrow is a programming error at the call site rather than a
    silent write), `TemplateReviewParkReason`, the 409-shaped error family
    (`ReviewStateError` carrying the *state* so the UI surfaces the transition,
    `ReviewVersionMismatchError`, `ReviewConflictError`, `PendingRevisionError`,
    `ApprovedPayloadHashMismatchError`, `ReservedActorError`), `SYSTEM_ACTOR` +
    `require_assignable_actor` on every client-facing mutation, `ensure_review_indexes`
    (partial unique `(caseId, requestId, reviewKind, scopeId)` over the *non-terminal*
    states = `case_review_open_scope_unique`; unique `(reviewId, actorId)` =
    `review_draft_edit_actor_unique`), and `ReviewAggregateStore`:
    `create_review` (check-then-act + index race), `request_revision`,
    `record_draft_revision`, `upsert_draft_edit` (coalescing row per actor,
    `client_edit_id` replay = no-op, `base_draft_version` guard, refused once past
    `OPEN` with the row left readable), `submit_edit` (sole-actor auto-promotion),
    `resolve_canonical_edit`, `conflict_marker` (case-scoped, versioned; bumped when a
    second actor's row appears, cleared by the canonical-edit write), `approve`
    (the atomic `OPEN → APPROVING`: plan command → lock review by CAS on state +
    both versions → insert command + outbox, one transaction; mints
    `logical_operation_id = review-delivery:{command_id}`, `delivery_id`,
    `content_hash`), `cancel`, `mark_sent`, `mark_delivery_failed`,
    `hold_for_operations`, `resume_from_hold`, `abandon` (audited), `retry_delivery`
    (`DELIVERY_FAILED → APPROVING` carrying the *stored* identity forward), `redraft`.
  - `backend/src/return_platform/operations/case_commands.py` — new public
    `insert_planned(planned, session=…)`. **Repair:** the WIP reached into
    `self._database["case_command_records"]` through a garbled expression
    (`planned.command_document and "case_command_records"` as a collection name —
    truthy by accident, meaningless by intent) and hard-coded `"integration_outbox"`
    beside it. Both collection names are `case_commands`' own knowledge; the pair-write
    now lives there, and the aggregate asks for it.
  - `backend/src/return_platform/operations/repository.py` — one call
    `ensure_review_indexes(self._db)` beside `ensure_case_command_indexes`, same
    one-place-for-index-creation precedent.
  - `backend/tests/operations/test_review_aggregate.py` (new, 38 tests).
- **Contract points consumed:** contracts.md sect. 6 entire (states, both `ABANDONED`
  arrows, `HELD_FOR_OPERATIONS → OPEN`, review kinds, edit model, canonical edit,
  conflict marker bump/clear, atomic approval with all four rejections + both version
  checks + `canonical_approved_payload_hash` under that exact name, frozen CAS → 409,
  `actor=SYSTEM` reserved, transactional co-location); sect. 7 delivery identity
  (`logical_operation_id → delivery_id → content_hash`, retry reuses all three).
- **Test result:** `tests/operations` — 692 passed, 126 deselected, 0 failed
  (654 → 692; the 38 are new). ruff check + format clean.
- **Deviation:** none new. The two standing ones (brief's `operations/return_case_recovery.py`
  path typo; append-only additions to S1's `fact_names.py`) carry forward unchanged.
- **Next step:** step:05 — delivery identity on the receiver side: unique partial index
  on `businessPayload.deliveryId` in `support_messages`, plus `ensure_case_support_thread`
  and `post_support_message` on `operations/return_support/service.py`.

---

## step:05 — receiver dedupe and the two thread ops

- **Files touched:**
  - `backend/src/return_platform/operations/return_support/service.py` —
    `SUPPORT_MESSAGE_DELIVERY_INDEX` + its declaration in `ensure_indexes`: unique on
    `businessPayload.deliveryId`, **partial** on `{"$type": "string"}` for exactly the
    reason the `sessionId` index above it carries in its own comment (unconditional,
    every message without a delivery id indexes as `null` and the second ordinary
    comment collides with the first). Two new result models (`CaseSupportThread`,
    `SupportMessagePost` — the latter's `absorbed` flag is how a caller learns a send
    was a redelivery, which is still success: absorption is delivery). Two new methods:
    `ensure_case_support_thread` (deliberately *not* a second opener — it delegates to
    `open_case_thread`, whose unique-`caseId`/loser-re-reads shape is already what the
    contract asks for, and adds the `thread_id` and the `created` flag the delivery
    callers need) and `post_support_message` (kind-agnostic, `delivery_id` checked twice:
    a read first because redelivery is the *expected* case on retry, the unique index
    second because the read cannot see a concurrent writer; `deliveryId` written last so
    a caller-supplied `business_payload` cannot displace it and silently opt out).
  - `backend/tests/operations/test_case_thread_delivery.py` (new, 10 tests) — index shape,
    the partial filter proven by two undeduped posts, thread-ensure idempotence, the
    two-creator race (pre-check blinded so the insert reaches the index), redelivery
    absorbed, the index absorbing what the pre-check could not see, two distinct
    deliveries both landing, delivery id undisplaceable, kind-agnosticism, unknown item.
- **Contract points consumed:** contracts.md sect. 7 delivery identity (receiver dedupe:
  unique partial index on `businessPayload.deliveryId` in `support_messages`) and thread
  ops (`ensure_case_support_thread` idempotent on `case_id` with the loser re-reading;
  `post_support_message` kind-agnostic, one deduped path for both review kinds).
- **Note for V1:** `post_support_message` raises nothing on a duplicate — it returns
  `SupportMessagePost(absorbed=True)` carrying the *winner's* message id and sequence.
  The review still moves to `SENT`. The work item's version is spent on an absorbed
  concurrent write (a gap in a display counter), which is the deliberate trade against
  a duplicate message in a person's queue.
- **Test result:** `tests/operations` — 702 passed, 126 deselected, 0 failed. ruff clean.
- **Deviation:** none new.
- **Next step:** step:06 — the `SupportAnalysisRecord` store under `operations/return_support/`.
