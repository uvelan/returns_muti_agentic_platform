# S2 ledger — delivery spine + review aggregate + context assembly

Append-only. One entry per step (contracts.md §3).

---

## step:01 — branch + anchor verification

- **Branch:** `feat/s2-delivery-spine` created off `7438e07084c33b92971671cd821db8424b828778` (S1 candidate head, per coordinator instruction; supersedes the brief's "RV-approved commit in merge.md" because the run is pipelined — S1 is under RV in parallel).
- **Files touched:** `.plan/tracks/S2.ledger.md` (this file), `.env` (gitignored copy from main checkout root for the test harness — not committed).
- **Commands:** `git checkout -b feat/s2-delivery-spine 7438e07`; smoke test run `python -m pytest tests/operations/test_durable_support_events.py` → 40 passed.
- **Anchors verified:**
  - `operations/support_events.py` — `DurableSupportEventStore.record_support_response`: unique `(caseId, supportEventId)` (`SUPPORT_EVENT_IDENTITY_INDEX`), `canonical_payload_digest`, outbox insert in the same Mongo transaction, `DuplicateKeyError` → winner re-read. MATCH.
  - `operations/integrations/temporal_signal.py` — `TemporalSignalDispatcher` with lazy client factory, permanent/transient RPC classification, fixed signal name ("the topic is the signal"). MATCH.
  - `operations/integrations/outbox.py` — `IntegrationOutboxDispatcher.claim()` lease CAS, `CLAIMABLE_STATUSES`, `DEAD_LETTER_STATUS`/`REQUIRES_RECONCILIATION`, `ensure_integration_outbox_indexes` as the one index owner. MATCH.
  - `operations/return_support/service.py` — `ensure_indexes` at 427–453 (unique `(threadId, sequence)` at 450–452; unique partial `caseId` at 440–444), `open_case_thread` at 606–758 (unique on `caseId`, loser re-reads — the brief's "~667–753" window covers its body), `post_reminder` at 760+ (dedupe on `businessPayload.reminderKey` at 778). MATCH (line offsets shifted slightly from the brief's approximations; content exact).
  - `workers/integration_outbox.py` — topic→dispatcher registration map, reconciliation sweep task. MATCH.
  - S1 interfaces consumed (read-only): `operations/fact_names.py` (constants only, docstring invites later slices to append their own), `operations/case_repository.py::append_scoped_case_fact` + `latest_case_facts_scoped`, `workflows/return_case_activities.py::append_scoped_fact_once` (`{fact_id}::{record_scope}` identity, `identity_version` stamp), `operations/artifact_binding.py`. MATCH.
  - **Anchor discrepancy (recorded, not adapted-around silently):** brief item 7 names `operations/return_case_recovery.py`; the module exists uniquely at `workflows/return_case_recovery.py` and its content matches the brief's description exactly (`ReturnCaseRecoveryService`, relaunch gated on confirmed-absent/closed executions, dead-letter reconciliation sweep). Treated as a path typo in the brief, not a contract mismatch: contracts.md §2 investigation 4 and §3 both place recovery in `workflows/`. Proceeding against the real path; flagged in the delta report for RV.
- **Test result:** smoke green (40 passed).
- **Next step:** step:02 — `operations/case_commands.py` (DurableCaseCommandStore + signal dispatcher + tests).

---

## step:02 — per-case stream ordering on the outbox

- **Files touched:**
  - `backend/src/return_platform/operations/integrations/outbox.py` — `CaseStream` enum (`inbound|outbound|review_commands|omc`), `CASE_STREAM_SEQUENCES_COLLECTION` CAS counter, `allocate_case_stream_sequence`, `ordered_command_fields` (enqueue validation: `UnknownPredecessorError` for a predecessor that doesn't exist on the case, `PredecessorCycleError` for self-reference — with existence-required + immutable predecessor lists, self-reference is the only expressible cycle), ordering fields on `OutboxCommand` (defaults = legacy commands unchanged), two new named partial unique indexes (`case_stream_sequence_unique` on `(aggregateId, stream, streamSequence)`, `case_stream_event_id_unique` on `eventId`), claim-path gate in `dispatch_once` (`_ordering_hold` → defer without burning an attempt / park stream on unresolved dead-lettered predecessor, incl. earlier-in-stream query so late enqueues into a parked stream are caught), `PARKED_STREAM` status (not claimable), `logger.error("case_stream_parked")` as the ops alert.
  - `backend/src/return_platform/workflows/return_case_recovery.py` — `CaseStreamRecovery` (audited operator skip/retry, CAS on `orderingResolved`; skip writes fact `support_stream_skip` via injected `ScopedCaseFactRepositoryPort` with `record_scope=None` and S1's `SCOPED_FACT_IDENTITY_VERSION` resolved lazily; retry requeues with attemptCount kept; both unpark).
  - `backend/src/return_platform/operations/fact_names.py` — **append-only** addition of `SUPPORT_STREAM_SKIP` and `CONTEXT_SUMMARY` constants. Recorded deviation: the coordinator said "never edit" S1 modules; the file's own S1-written docstring says "Later slices append their own as they earn them", and the contract forbids fact-name string literals outside this file — so referencing these facts at all requires the append. Append-only, existing lines untouched. Flagged for RV.
  - `backend/tests/operations/mongo_double.py` (new shared double: dotted paths, `$ne`/`$gte`, upsert, `update_many`), `backend/tests/operations/test_case_stream_ordering.py` (new), `backend/tests/operations/test_integration_outbox_indexes.py` (EXPECTED_UNION extended with the two new declarations — the file's own stated maintenance path).
- **Commands:** pytest on the four touched/adjacent test files; ruff check+format.
- **Test result:** 93 passed (ordering 15, support events 40, index union 9, recovery reconciliation suite green).
- **Anchors verified:** `claim()` filter uses `CLAIMABLE_STATUSES` (`PARKED_STREAM` excluded by construction); `_dead_letter` semantics untouched.
- **Next step:** step:03 — `operations/case_commands.py`.
