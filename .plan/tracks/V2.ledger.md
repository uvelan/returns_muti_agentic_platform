# V2 ledger — support ingress + B→A relay (phase 1: backend)

Append-only. One entry per step; one commit per step.

Phase 2 (brief item 6 — frontend panel sections, `ConversationPane` rendering,
MSW) is **BLOCKED and not attempted**: V1's panel section-registry seam does not
exist on this base. Nothing under `frontend/` is touched by this slice.

---

## step:01 — branch off S2's round-2 candidate head; anchors verified; ledger opened

**Files:** `.plan/tracks/V2.ledger.md` (new).

**Branch:** `feat/v2-ingress-relay`, created off `c884e8e`
(`(S2) step:11 the conflict marker moves with the review, or not at all`) —
S2's round-2 candidate head on `feat/s2-delivery-spine`, exactly as dispatched.

**Anchors verified before any writing** (contracts §3 — mismatch is a halt, never
an adaptation). Every one matched:

| anchor (brief / contracts) | verified at | outcome |
| --- | --- | --- |
| `api/return_support.py:597 submit_return_outcome` | line 597, `async def submit_return_outcome` | exact line match |
| `api/return_support.py::_support_event_id` | line 520 | present; body/header-or-refuse semantics as described |
| `ReturnOutcomeRequest` / `ReturnOutcomeRecord` | `api/return_support.py:477` / `:406` | record-list shape as §5 assumes |
| `operations/support_events.py` `DurableSupportEventStore` | line 249 | one-transaction event+outbox; `canonical_payload_digest`, `IdempotencyConflictError`, `SupportEventReceipt` all present |
| S2 `SupportAnalysisRecordStore` loop | `operations/return_support/analysis_records.py:192` | `ensure_record`/`pin_routing_decision`/`next_candidate_route`/`record_attempt`/`accept_result`/`block_exhausted` and `require_accepted_extraction` (line 169) all match the signatures dispatched to me |
| `accept_result -> (accepted, is_new)` | line 382 | confirmed: `is_new=False` means a result was already accepted and this one was discarded |
| S2 thread ops | `operations/return_support/service.py:858` / `:914` | `ensure_case_support_thread -> CaseSupportThread{workItemId,threadId,created}`; `post_support_message -> SupportMessagePost{...,absorbed}` |
| S2 ordering machinery | `operations/integrations/outbox.py` | `CaseStream` (39), `ordered_command_fields` (346), `allocate_case_stream_sequence` (315), `UnknownPredecessorError` (68), `PredecessorCycleError` (87), `OutboxCommand.{event_id,causation_id,required_predecessor_ids}` (102) |
| S1 `operations/artifact_binding.py` | whole file | `bind_artifact`/`bind_artifacts`/`persist_binding_decision`, `ArtifactType`, `BindingStatus`, ports — consumed, never edited |
| `operations/fact_names.py` | whole file | 4 constants today; docstring explicitly invites later slices to append |
| `ai/gateway/structured_invocation.py::StructuredOutputInvoker` | line 279 | constructor + re-resolved `task` property as briefed |
| `AtomicConversationRepository` | `dynamic_knowledge/order_agent/conversation_repository.py:97` | present |
| `workers/integration_outbox.py` dispatch table | lines 73–144 | `dispatchers: dict[str, TopicDispatcher]`, topic-keyed |
| `security/capabilities.py::RETURNS_SUPPORT_ACT` | line 37 | present in the support role bundle (165) |
| `configuration/return_configuration.py` field-append point | line 1706 (`context_assembly`) | one-line import + field append shape confirmed |
| `backend/config/ai_gateway.yaml` task shape | lines 66+, 589+ | `systemPromptSections` + `promptVersion` + `allowedInputKeys` confirmed |

**Commands:** `git checkout -b feat/v2-ingress-relay c884e8e`;
`python -m pytest tests/operations/test_support_analysis_records.py tests/operations/test_case_stream_ordering.py -q`.

**Test result:** 35 passed — the consumed S2 substrate is green on this base, so
anything red later is mine.

**Next step:** `support_ingress` configuration module + its integration append.

---

## step:02 — `support_ingress` configuration + its integration append

**Files:** `backend/src/return_platform/configuration/support_ingress_configuration.py`
(new, owned by V2); `backend/src/return_platform/configuration/return_configuration.py`
(integration duty: one-line import + one defaulted field append, nothing else);
`backend/config/returns/production.yaml` (integration duty: defaulted block);
`backend/tests/configuration/test_support_ingress_configuration.py` (new).

**Shape** — contracts §10 verbatim: `support_ingress{nl_enabled, intents[],
parking{retention, per_case_quota, alert_dedupe_window},
multi_record_framing_prompt_key, agent_disclosure{display_name, disclosure_line},
outbound_templates{}, limits{}}`.

`nl_enabled` defaults **false**. The parked lifecycle (§5) is what makes that
free: a message arriving while the door is shut is persisted, never refused, and
reprocessed in stream order when the switch flips.

**Anchors verified:** `return_configuration.py` field-append point (the
`context_assembly` block, S2's precedent for the same one-line shape);
`production.yaml` block placement; the `StrictConfigModel` cycle-avoidance
rationale carried over from `context_assembly_configuration.py`.

**Commands:** `python -m pytest tests/configuration/test_support_ingress_configuration.py -q`
→ **5 passed**. `ruff check backend/src/return_platform/configuration/ …` → clean.

**A1 watch:** no fact write in this step. Recorded for completeness.

**Next step:** `NormalizedSupportEvent` + the two normalizers.
