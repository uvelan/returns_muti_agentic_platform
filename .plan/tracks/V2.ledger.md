# V2 ledger — support ingress + B→A relay (phase 1: backend)

Append-only. One entry per step; one commit per step.

Phase 2 (brief item 6 — frontend panel sections, `ConversationPane` rendering,
MSW) is **BLOCKED and not attempted**: V1's panel section-registry seam does not
exist on this base. Nothing under `frontend/` is touched by this slice.

---

## step:01 — branch off S2's round-2 candidate head; anchors verified; ledger opened

**Files:** `.plan/tracks/V2.ledger.md` (new).

**Branch:** `feat/v2-ingress-relay`, created off `c884e8e`
(`(S2) step:11 the conflict marker moves with the review, or not at all`) —
S2's round-2 candidate head on `feat/s2-delivery-spine`, exactly as dispatched.

**Anchors verified before any writing** (contracts §3 — mismatch is a halt, never
an adaptation). Every one matched:

| anchor (brief / contracts) | verified at | outcome |
| --- | --- | --- |
| `api/return_support.py:597 submit_return_outcome` | line 597, `async def submit_return_outcome` | exact line match |
| `api/return_support.py::_support_event_id` | line 520 | present; body/header-or-refuse semantics as described |
| `ReturnOutcomeRequest` / `ReturnOutcomeRecord` | `api/return_support.py:477` / `:406` | record-list shape as §5 assumes |
| `operations/support_events.py` `DurableSupportEventStore` | line 249 | one-transaction event+outbox; `canonical_payload_digest`, `IdempotencyConflictError`, `SupportEventReceipt` all present |
| S2 `SupportAnalysisRecordStore` loop | `operations/return_support/analysis_records.py:192` | `ensure_record`/`pin_routing_decision`/`next_candidate_route`/`record_attempt`/`accept_result`/`block_exhausted` and `require_accepted_extraction` (line 169) all match the signatures dispatched to me |
| `accept_result -> (accepted, is_new)` | line 382 | confirmed: `is_new=False` means a result was already accepted and this one was discarded |
| S2 thread ops | `operations/return_support/service.py:858` / `:914` | `ensure_case_support_thread -> CaseSupportThread{workItemId,threadId,created}`; `post_support_message -> SupportMessagePost{...,absorbed}` |
| S2 ordering machinery | `operations/integrations/outbox.py` | `CaseStream` (39), `ordered_command_fields` (346), `allocate_case_stream_sequence` (315), `UnknownPredecessorError` (68), `PredecessorCycleError` (87), `OutboxCommand.{event_id,causation_id,required_predecessor_ids}` (102) |
| S1 `operations/artifact_binding.py` | whole file | `bind_artifact`/`bind_artifacts`/`persist_binding_decision`, `ArtifactType`, `BindingStatus`, ports — consumed, never edited |
| `operations/fact_names.py` | whole file | 4 constants today; docstring explicitly invites later slices to append |
| `ai/gateway/structured_invocation.py::StructuredOutputInvoker` | line 279 | constructor + re-resolved `task` property as briefed |
| `AtomicConversationRepository` | `dynamic_knowledge/order_agent/conversation_repository.py:97` | present |
| `workers/integration_outbox.py` dispatch table | lines 73–144 | `dispatchers: dict[str, TopicDispatcher]`, topic-keyed |
| `security/capabilities.py::RETURNS_SUPPORT_ACT` | line 37 | present in the support role bundle (165) |
| `configuration/return_configuration.py` field-append point | line 1706 (`context_assembly`) | one-line import + field append shape confirmed |
| `backend/config/ai_gateway.yaml` task shape | lines 66+, 589+ | `systemPromptSections` + `promptVersion` + `allowedInputKeys` confirmed |

**Commands:** `git checkout -b feat/v2-ingress-relay c884e8e`;
`python -m pytest tests/operations/test_support_analysis_records.py tests/operations/test_case_stream_ordering.py -q`.

**Test result:** 35 passed — the consumed S2 substrate is green on this base, so
anything red later is mine.

**Next step:** `support_ingress` configuration module + its integration append.

---

## step:02 — `support_ingress` configuration + its integration append

**Files:** `backend/src/return_platform/configuration/support_ingress_configuration.py`
(new, owned by V2); `backend/src/return_platform/configuration/return_configuration.py`
(integration duty: one-line import + one defaulted field append, nothing else);
`backend/config/returns/production.yaml` (integration duty: defaulted block);
`backend/tests/configuration/test_support_ingress_configuration.py` (new).

**Shape** — contracts §10 verbatim: `support_ingress{nl_enabled, intents[],
parking{retention, per_case_quota, alert_dedupe_window},
multi_record_framing_prompt_key, agent_disclosure{display_name, disclosure_line},
outbound_templates{}, limits{}}`.

`nl_enabled` defaults **false**. The parked lifecycle (§5) is what makes that
free: a message arriving while the door is shut is persisted, never refused, and
reprocessed in stream order when the switch flips.

**Anchors verified:** `return_configuration.py` field-append point (the
`context_assembly` block, S2's precedent for the same one-line shape);
`production.yaml` block placement; the `StrictConfigModel` cycle-avoidance
rationale carried over from `context_assembly_configuration.py`.

**Commands:** `python -m pytest tests/configuration/test_support_ingress_configuration.py -q`
→ **5 passed**. `ruff check backend/src/return_platform/configuration/ …` → clean.

**A1 watch:** no fact write in this step. Recorded for completeness.

**Next step:** `NormalizedSupportEvent` + the two normalizers.

---

## step:03 — `NormalizedSupportEvent` and the two normalizers

**Files:** `backend/src/return_platform/operations/return_support/ingress.py` (new);
`backend/tests/operations/test_support_ingress_normalization.py` (new).

**What landed.** The §5 internal contract as a frozen dataclass, plus
`normalize_return_outcome` (structured; **no model** — intent is assigned from
the payload's own shape, `rma_issued` or `rejection`, both frozen-taxonomy
members) and `normalize_inbound_message` (natural language; intent and artifacts
deliberately empty until an accepted analysis is folded in through
`with_analysis`). `derive_support_event_id` mints the internal id by uuid5 over
the contract's three-part dedupe identity, length-prefixed so
`("ab","c")` and `("a","bc")` cannot collide. `coerce_intent` enforces the closed
set in code over the *released* list, so widening the config widens what is
recognised and never what runs.

**Anchors verified:** `support_return_record` (`operations/support_events.py:155`)
— record groups are built through it, not by a dict literal, so the snake-case
contract with the workflow dataclass stays in one place; `ArtifactType` /
`ExtractedArtifact` (`operations/artifact_binding.py`, S1, consumed unmodified).

**Commands:** `python -m pytest tests/operations/test_support_ingress_normalization.py -q`
→ **20 passed**. `ruff check` → clean.

**Fault injection (guarantee: structured and NL normalize to identical events;
dedupe identity is three-part; taxonomy is closed; DR-11 create-never).**
Harness mutates the source, runs the selection, restores. Baseline 20 passed.

| injected fault | result |
| --- | --- |
| A — canonical form stops sorting record groups (arrival order becomes a business fact) | **CAUGHT** 1 failed |
| B — `_optional` returns `""`, so a blank erases a value Support never mentioned | **CAUGHT** 1 failed |
| C — event-id derivation joins identity parts without length prefixes | **CAUGHT** 1 failed |
| D — `coerce_intent` passes any model answer through (taxonomy stops being closed) | **CAUGHT** 3 failed |
| E — a group with a blank `returnReference` becomes a record (DR-11 create-never breaks) | **CAUGHT** 1 failed |
| F — `transport_id` drops out of the dedupe identity (two transports collapse to one event) | **CAUGHT** 2 failed |
| G — the NL normalizer guesses an intent instead of leaving the message unanalysed | **CAUGHT** 1 failed |

Restored → 20 passed. Note the pairing that matters: the equivalence test is
worthless without
`test_the_canonical_form_still_separates_events_that_say_different_things`,
which is what fault A/B/E actually trip — a canonical form returning `{}` would
pass the equivalence assertion alone.

**A1 watch:** no fact write in this step.

**Next step:** the durable ingress store — raw + event + outbox row in one
transaction, with the causation chain populated.

---

## step:04 — the durable ingress store: raw + event + outbox in one act, and the causation chain filled

**Files:** `backend/src/return_platform/operations/return_support/ingress_store.py`
(new); `backend/tests/operations/test_support_ingress_store.py` (new).

**Shape.** `DurableSupportIngressStore` **subclasses** `DurableSupportEventStore`
rather than copying it — the brief's "`DurableSupportEventStore` extension
(anchor `operations/support_events.py`)". Subclassing keeps V2's code in V2's
file while the digest semantics, the no-op/409 rule and the outbox document shape
stay single-sourced in the anchor module. **No line of `support_events.py` was
edited.**

Collections: `case_support_inbound_messages` (unique on the contract's
`(caseId, transportId, externalMessageId)` *and* on the derived
`supportEventId`), `support_parking_alerts` (the dedupe window as stored state,
not process state — otherwise the operator's inbox resets on every deploy).
Topic `return-case.support-message.classify` (contracts §7's new topic list).

**Carry-forward condition — acceptance 18's causation chain.** `causation_id` and
`required_predecessor_ids[]` are populated here, on every enqueued inbound event,
from the tail of the case's inbound chain read out of the **outbox** (a parked
message has no stream event yet; naming one would be `UnknownPredecessorError` at
best and a permanently-held dependent at worst). Both fields are filled, not just
the list: causation says why the event exists, the predecessor list says what must
finish first, and they diverge as soon as an event is caused from another stream.

The chain is tested **through S2's real `IntegrationOutboxDispatcher`**, with the
queue loaded against the answer: the newest command is made the oldest-due, so
`claim`'s `(nextAttemptAt, createdAt)` sort offers message three first. Only
message one is delivered; two and three are claimed, refused by `_ordering_hold`,
and released with `WAITING_ON_PREDECESSOR`. That is the drain *and* the chain,
not just the drain.

**Parked lifecycle.** Shut door → persisted `PARKED`, no command, never a 409;
redelivery of a parked message is still `duplicate=True` and still parked. The
backlog drains **on the accept path**, ahead of the new message's own commit, so
"parked reprocessed in stream order before new ones" needs nobody to remember to
run anything. Quota breach escalates *through* the alert dedupe window, because a
case past quota is not routine parking.

**Anchors verified:** `ordered_command_fields` / `allocate_case_stream_sequence` /
`CaseStream.INBOUND` / `WAITING_ON_PREDECESSOR` (S2, `operations/integrations/outbox.py`,
consumed unmodified); `canonical_payload_digest`, `IdempotencyConflictError`,
`SUPPORT_EVENT_AGGREGATE_TYPE`, `DurableSupportEventStore` (`operations/support_events.py`).

**Commands:** `python -m pytest tests/operations/test_support_ingress_store.py -q`
→ **12 passed**. `ruff check` → clean.

**Fault injection (guarantees: dedupe, ordered drain, parked-never-refused,
raw-body integrity).** Baseline 12 passed.

| injected fault | result |
| --- | --- |
| A — `required_predecessor_ids` left empty (sequence numbers only) | **CAUGHT** 3 failed |
| B — `causation_id` dropped | **CAUGHT** 1 failed |
| C — the parked backlog is not drained before a new message is accepted | **CAUGHT** 1 failed |
| D — a digest mismatch is absorbed instead of refused | **CAUGHT** 1 failed |
| E — a parked message is enqueued for classification anyway (the shut door leaks) | **CAUGHT** 4 failed |
| F — the raw body is dropped (evidence replaced by interpretation) | **CAUGHT** 3 failed |
| G — the alert-dedupe window boundary made exclusive (a window that never reopens) | **CAUGHT** 1 failed |
| H — the quota compared with `>=` (escalates one message early) | **CAUGHT** 1 failed |
| I — the drain releases the backlog newest-first | **CAUGHT** 1 failed |
| J — the chain tail read oldest-first, so every event names the *first* event | **CAUGHT** 2 failed |

Restored → 12 passed. Fault A is the one the carry-forward condition was written
about: with the predecessor list emptied the drain still "works" — every command
delivers — and three tests fail, because they assert the *hold*, not the outcome.

**A1 watch:** no fact write in this step. The store writes its own collections
only; nothing reaches `case_facts`.

**Next step:** the NL ingress endpoint.
