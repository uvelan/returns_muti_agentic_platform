# V3 ledger — resolver + clarification round-trip

Append-only. One entry per step, per contracts.md §3.

Branch `feat/v3-resolver-clarification`, base `3715cbe` (trunk = S1 + ACC-1 +
V1 phase 1 + S2, all merged and RV-approved; merged with V2 candidate
`e37b494`).

---

## step:00 — base verification

**Repo state on arrival.** The worktree was checked out on
`worktree-agent-a2a2c45f5efd72b5f` at `0448d32`, an unrelated old commit —
`.plan/` did not exist there. `feat/v3-resolver-clarification` existed as a
branch with head `3715cbe9bf02bfd910bacd1561308add675291d9`, which **matches
the dispatched head exactly**. Checked the worktree out onto it. Not a halt:
the branch content matched; only the worktree's checkout pointer was stale.

**Anchors verified present before any writing:**

| Anchor | Path | Verdict |
|---|---|---|
| Checkpointer | `platform/reasoning/checkpoint.py` — `SystemStoreCheckpointSaver` | present |
| Capability registry | `platform/capabilities/{contracts,registry}.py` — `CapabilityName`, `CapabilityRegistry`, `InMemoryCapabilityRegistry` | present |
| On-demand sync | `dynamic_knowledge/on_demand_sync/coordinator.py` | present |
| Context assembly (S2) | `platform/reasoning/case_context.py` — `assemble_case_context(facts, policy, *, extra_pinned_fact_names=())` → `AssembledContext{content_hash, consumed_fact_ids, omitted_fact_ids, compaction_recommended, .payload()}` | present, signature matches dispatch |
| Review aggregate (S2) | `operations/review_aggregate.py` — `ReviewKind.SUPPORT_REPLY`, `create_review(..., scope_id=None)` mints server-side for SUPPORT_REPLY, `approve(...)`, `ReviewStateError.state` | present, matches dispatch |
| Command store (S2) | `operations/case_commands.py` — `DurableCaseCommandStore.plan_command`/`insert_planned`/`record_command`, `CaseCommandKind.CLARIFICATION_ANSWERED`, `SIGNAL_FOR_COMMAND_KIND` | present |
| Neutralisation (V1/composition) | `operations/support_handoff.py` — `_FRAMING`, `_safe`, `_clean` | present, importable |
| Fact names (S1, append-only) | `operations/fact_names.py` — 7 constants today | present |
| V2 ingress | `operations/return_support/{ingress,message_classification,analysis_wiring,ingress_store}.py` | present |
| Config | `configuration/{support_ingress,support_template,context_assembly}_configuration.py` | present |

Config model loads with `support_template`, `context_assembly`,
`support_ingress` fields present and `support_resolver` absent (V3 adds it).

**Harness.** No venv in the worktree. Using the main checkout's
`backend/.venv/Scripts/python.exe` with
`PYTHONPATH=<worktree>/backend/src`; gitignored root `.env` copied in.

---

## step:01 — placement verdict: the existing responder cannot carry the ladder

Required first step (brief item 1, contracts.md §9: *"inspect and extend
`operations/return_support/auto_responder.py` + `agents/support_response.py`
first; a new graph only on a ledger-recorded verdict"*). Both files read in
full — 281 and 225 lines respectively.

### What they actually are

`SupportResponseAgent` (`agents/support_response.py`) **plays Support**. It is
a deterministic simulator of the Support desk: given a `support-handoff-v1`
payload that *the platform sent outbound to Channel B*, it plans what Support
would issue back — RMA reference, tracking, label, shipping instruction —
derived from the released `return_policy.return_method_requirements` table via
`_reference()`, a pure string derivation over the case id. There is no model
invocation anywhere in the file. `confidenceMillionths` is a hard-coded
`950_000` / `1_000_000` literal on the decision view, not a measured
confidence.

`SupportAutoResponder` (`operations/return_support/auto_responder.py`)
executes that plan **on the Support side of the boundary**: acknowledges the
Support *work item*, posts the agent's message to the Support thread, and
records the outcome through `DurableSupportEventStore.record_support_response`
— the same seam `submit_return_outcome` uses. Its inputs are a
`work_item_id`, a `SupportWorkItemView` and a thread message whose
`businessPayload.schemaVersion == "support-handoff-v1"`.

### Verdict: **they cannot carry the resolution ladder.** Six independent reasons.

1. **Opposite direction across the channel.** These two answer *the
   platform's outbound handoff, as Support*. V3's resolver answers *Support's
   inbound question, on the associate's behalf* — the A-side of the bridge.
   The `SUPPORT_RESPONSE_ACTOR = "support-response-agent"` provenance stamped
   on every outcome and acknowledgement is the Support desk's identity;
   reusing it for an A-side answer would make the platform's own reply
   indistinguishable from Support's, which is exactly the provenance loss RV
   rule 6 names. `agents/support_response.py`'s own module docstring states
   the boundary: *"Plans what Support would issue for a handed-off return."*

2. **Wrong input contract, with no adapter that preserves meaning.**
   `_handoff_request` reads a `support-handoff-v1` business payload keyed to a
   work item and, by its own documented rule, *"reads the structured half of
   the handoff only — never the message text"*. V3's input is V2's
   `NormalizedSupportEvent` keyed `(case_id, support_event_id)`, whose whole
   substance **is** `body_text` plus a classified intent. The one rule that
   file states about its input is the rule V3's input violates.

3. **No reasoning plane at all.** The ladder requires: an LLM invocation
   (`support.question.resolve.v1` via `StructuredOutputInvoker`), measured
   confidence compared against `fact_confidence_millionths` /
   `graph_confidence_millionths`, a graph read with its own on-demand sync, a
   tool step, and a per-case LLM budget. `SupportResponseAgent.assess` is a
   synchronous pure function over a requirement table. Every one of those five
   is a new concern; nothing in either file is reused by adding them.

4. **No checkpointing, and no place to put it.** §9 requires
   `SystemStoreCheckpointSaver` on thread `support-resolver:{case_id}:{support_event_id}`
   with resume-at-last-completed-node (acceptance 23). `assess()` is a single
   synchronous call with no nodes and no interruptible boundary; `respond()`
   is idempotent by *re-reading and recognising its own writes*, which is a
   different and weaker guarantee than checkpoint resume — it re-executes the
   whole body every time. Retro-fitting durable node boundaries into
   `respond()` would rewrite it entirely.

5. **Keyed to the wrong identity, in a way that breaks the checkpoint thread.**
   `support_event_id_for(work_item_id)` mints
   `f"support-response-agent:{work_item_id}"` — the support event id is
   *derived from the work item*. V3's thread id needs V2's
   `support_event_id`, which is derived from
   `(case_id, transport_id, external_message_id)` (`derive_support_event_id`).
   Two different id algebras for the same field name.

6. **Frozen-module adjacency and the status guard.** `_ACTIONABLE` refuses to
   act unless the work item is `NEW | ACKNOWLEDGED | IN_PROGRESS`, on the
   documented reasoning that *"a second answer would talk over it"*. A V3
   resolution of an inbound support question is legitimate on a thread whose
   work item is long past those states — the resolver answers a *message*, not
   a *work item*. Loosening that guard would weaken a shipped safety property
   of the Support-side path for a reason having nothing to do with it.

**Therefore:** a new graph module. Neither file is modified; neither is
imported by V3 except `operations/support_handoff.py`'s `_safe`, which is
composition's neutraliser and is imported rather than re-implemented
(carry-forward condition 7).

**Files touched:** `.plan/tracks/V3.ledger.md` (new).
**Commands:** reads only; config-load check.
**Next:** step:02 — `support_resolver` configuration module.

---

## step:02 — the released resolution policy, and a schema allowlist config cannot author

**New files.**
- `platform/capabilities/tool_schemas.py` — the code-side allowlist of tool
  input schemas. Two schemas: `graph.return_record_lookup.v1`,
  `graph.shipment_status.v1`. Both require `caseId`, so no binding can ever be
  routed at a record belonging to another case.
- `configuration/support_resolver_configuration.py` —
  `SupportResolverConfiguration`, `ToolBindingConfiguration`,
  `ReplyGateConfiguration`.
- `tests/configuration/test_support_resolver_configuration.py` — 20 tests.

**Integration append applied in-slice** (my own config, per the dispatch's
integration duty): one import + one field on `return_configuration.py`.

### The design decision that carries the tool boundary

§9 says config maps intent → eligible capabilities and that arguments are
validated against `input_schema_ref`. The load-bearing choice is that
`input_schema_ref` **names an entry in a code-side allowlist and cannot define
one** — the same device §8 uses for the formatter allowlist. A release's
strongest possible statement about a tool is "this implementation the build
already has is now reachable for this intent". There is no field on
`ToolBindingConfiguration` that holds a value, an expression or a template;
all seven fields are references, and `extra="forbid"` means a release cannot
add one.

`EntityField.coerced` is a type *check*, not a conversion — a trusted fact of
the wrong shape reads as a missing entity (which the router refuses on) rather
than being coerced into something a tool would accept. `bool` is excluded from
`INTEGER` explicitly, because `isinstance(True, int)` is true in Python and
without that line a boolean fact would reach a tool as the quantity `1`.

Defaults are all closed: thresholds 900,000 millionths, `tool_bindings = ()`
(no tool eligible for any intent), `reply_gate.default = review_required`,
`per_case_llm_budget = 12`.

### Fault-injection evidence (11 faults, all caught)

Baseline `20 passed`. Each fault applied alone and reverted:

| Fault | Result |
|---|---|
| A drop the `input_schema_ref` parse-time validator | 1 failed |
| B gate mode becomes an open `str` | 1 failed |
| C budget default 12 → 999 | 1 failed |
| C2 fact threshold 900k → 500k | 1 failed |
| C3 reply-gate default → `auto_reply` | 2 failed |
| D drop the `bool` guard in `coerced()` | 1 failed |
| E a binding may be eligible for no intent | 1 failed |
| F `bindings_for_intent` ignores the intent | 1 failed |
| G unknown schema ref returns an empty schema instead of raising | 1 failed |
| H a schema stops requiring `caseId` | 1 failed |
| I `ToolBindingConfiguration` stops forbidding extra fields | 1 failed |

Restored: `20 passed`. The defaults test is a single tuple equality rather than
seven separate assertions, so a future edit cannot loosen one default while the
others keep it green — C, C2 and C3 each prove that independently.

**Anchors verified:** `CapabilityName`/`CapabilityRegistry` shape,
`CredentialBindingConfiguration.profile_key` as the credential identity,
`support_ingress_configuration`'s `StrictConfigModel` cycle-avoidance pattern.
**Next:** step:03 — the tool router.

---

## step:03 — the tool boundary, made structural rather than disciplinary

**New files.** `platform/capabilities/tool_router.py`,
`tests/platform/test_support_tool_router.py` (22 tests).

### How §9's wording became implementable

§9 separates *selection* from *argument supply*, and both are now structural.

**Selection.** `plan_tool_invocation` takes a `ValidatedIntent` — a frozen
dataclass whose `__post_init__` raises `UnvalidatedIntentError` unless it was
built by `validate_intent`, which checks membership in the released closed
taxonomy. A string of support prose cannot become one. The widest thing any
text can do is land on one of the nine taxonomy members, each of which selects
from a list a release wrote.

**Argument supply.** `plan_tool_invocation(intent, bindings, trusted)` — three
parameters, **none of them text**. It cannot take an argument from a support
message because it has nowhere to receive one. `TrustedEntities` is built only
by `trusted_entities_from(*, case_facts, fact_ids, graph_results)`, which is
keyword-only over exactly the two sources §9 permits. Both signatures are
asserted by `inspect.signature`, so adding a `body_text=` parameter later fails
a test rather than passing quietly.

Additional properties, each with its own test: only fields the schema declares
are filled (an extra trusted fact cannot widen a tool's input); a case fact
beats a graph result of the same name (a disagreement is a *conflict* the
ladder escalates, not a precedence the router settles); the executor
**re-validates the schema** rather than trusting the plan, because a plan
crosses a checkpoint boundary and a stale plan against a moved schema is
exactly what "execution requires schema validation" refuses; authorization runs
before the port is touched; the contract *name* resolves through a code-side
allowlist, so a released string cannot name an arbitrary type; and the port
receives `credential_binding_id`, never a value — there is no field on the plan
or the port through which a secret could travel.

### Why the hostile-input test is not a "does not contain" assertion

`HOSTILE_BODY` names a tool, a capability, a contract, a credential id, two
argument values and a framing-shaped heading. The central assertion pins the
**whole composed plan as one equality** —
`ToolInvocationPlan(tool_id=…, arguments={"caseId": "case-REAL",
"returnReference": "RMA-REAL"}, argument_provenance={…}, …)`. A negative
assertion would still pass if the router grew a *different* way to take an
argument from text; this equality fails the moment any field is not exactly
what the trusted bag and the released binding produced.

### Fault-injection evidence (13 faults, all caught)

Baseline `22 passed`. Each applied alone and reverted:

| Fault | Result |
|---|---|
| A `ValidatedIntent` stops guarding its constructor | 1 failed |
| B `validate_intent` accepts anything | 1 failed |
| C the router accepts a bare string as an intent | 1 failed |
| D undeclared trusted entities pass through as arguments | 1 failed |
| E a missing required entity no longer refuses | 4 failed |
| F the executor trusts the plan's schema instead of re-checking | 1 failed |
| G authorization is skipped | 1 failed |
| H the contract allowlist is bypassed | 1 failed |
| I graph results override case facts | 1 failed |
| J the refusal loses the missing-entity names | 3 failed |
| K argument provenance is dropped | 3 failed |
| L the executor passes the credential id as an argument | 1 failed |
| M eligibility ignores the intent | 1 failed |

Restored: `22 passed`.

**Next:** step:04 — the resolution ladder graph, with neutralisation.

---

## step:04 — carry-forward condition 7, discharged

**New files.** `operations/return_support/outbound_composition.py`,
`tests/operations/test_support_outbound_composition.py` (24 tests).

### How condition 7 is discharged

Three composed messages carry words a person typed: the clarification prompt
shown to the associate (carrying Support's question **verbatim**), the
clarification relay back to Support (carrying the associate's answer), and the
reply draft. These are the two shapes the dispatch called the riskiest in the
design.

**The rule is stronger than four-value parity, deliberately.** Composition
`_safe`s exactly four values on the argument "these are the human-typed ones".
That argument has to be re-derived every time a field is added, and V1's defect
is exactly what happens when it is derived once and not repeated. Here:

- section text is a **code constant** — `_QUESTION_HEADING` and friends are
  `Final` strings in this module, never configuration and never a fact;
- **every interpolated value is neutralised, without exception** —
  `render_section(heading, *values)` is the only way to build a line and it
  neutralises unconditionally. Its signature has no parameter for pre-rendered
  text, so no caller can hand it a string it already formatted;
- model-authored reply text is neutralised too, on the reasoning that a model
  which has just read the untrusted input is the most likely thing in the
  system to reproduce it;
- the released **disclosure line** is neutralised (see the blind test below);
- `render_configured_template` neutralises every value **before** substitution,
  and substitution happens once over the template so a value carrying a brace
  cannot re-enter the grammar.

`_safe` is **imported** from `support_handoff.py`, never re-implemented — V1's
own fix, and what makes a second spelling impossible. A test asserts
`outbound_composition._safe is _safe`, so a local copy fails. Parity is stated
over the **pattern** (`_FRAMING.search(shape)`) rather than over a list of four
field names, so a widened `_FRAMING` is covered without an edit here.

### "Verbatim" and neutralisation are not in conflict

The **fact** stores the question exactly as Support sent it — that is the audit
record and nothing here touches it (pinned by a test asserting the input string
is unmutated). The **rendered message** neutralises, and neutralisation only
rewrites a line that *is itself* a heading or separator. An ordinary support
question passes through **byte for byte**, pinned as a whole-output equality,
not asserted. Nothing a support agent could legitimately write is lost: a
question cannot be asked by a bare `SHIPPING INSTRUCTION:` line. A companion
test pins that neutralisation is narrow — `RMA-4471`, `Bay 7`,
`The label is in the bay: check it` and `1Z999AA1` are untouched.

Every hostile-input assertion pins the **whole composed output as an equality**.
No "does not contain" assertion appears in the file.

### Fault-injection evidence (12 faults) — including one my own test was blind to

Baseline `23 passed`.

| Fault | Result |
|---|---|
| A the exact V1 defect — `neutralized(x)` becomes `str(x)` | 13 failed |
| B `_safe` re-implemented locally instead of imported | 6 failed |
| C only the answer neutralised, not the question | 1 failed |
| D model-authored reply text trusted because an agent wrote it | 1 failed |
| E candidate record references skip the neutraliser | 1 failed |
| F neutralisation widens and eats ordinary content | 5 failed |
| G `render_section` grows a pre-rendered-text parameter | 1 failed |
| H `discloses_agent` derived from the text instead of carried | 2 failed |
| I a blank value renders as an empty section | 5 failed |
| J configured templates interpolate without neutralising | 1 failed |
| K template substitution recurses into substituted values | 1 failed |
| **L the disclosure line is exempted from neutralisation** | **23 passed — BLIND** |

**Fault L survived the first pass.** Exempting the released disclosure line left
one un-neutralised path into a Channel B message and every test stayed green —
the same class of gap the reviewer has found in every slice, found here by my
own injection. Fixed by
`test_the_disclosure_line_itself_is_neutralised`, which pins the whole composed
output with a hostile `display_name` and `disclosure_line`. Re-run: **L → 1
failed**, all 12 caught, baseline `24 passed`.

The reasoning that had made the exemption feel safe — "the disclosure is
released configuration, not a person's typing" — is exactly how the one
exemption gets made. A released string that can restructure an outbound message
is still a released string that can restructure an outbound message.

**Next:** step:05 — the resolution ladder graph.

## step:05 — the resolution ladder, and two blind spots my own tests had

**New files.** `operations/return_support/resolution_state.py`,
`operations/return_support/resolution_ladder.py`,
`tests/operations/test_support_resolver_state.py` (19 tests),
`tests/operations/test_support_resolution_ladder.py` (24 tests).
**Appended.** `operations/fact_names.py` — `SUPPORT_RESOLVER_BUDGET_EXHAUSTED`
(added in the step that first references it, per the module's own rule).

### Placement inside the tree

`operations/return_support/`, **not** `agents/`. `tests/agents/
test_no_cross_agent_imports.py` enumerates seven flat agent modules and
`agents/README.md` carries a per-agent contract (`<name>.md`, registry entry);
the resolver is not one of the seven orchestrated case agents, and adding it
there would have meant conforming to a convention that says nothing about it.
It sits beside its actual collaborators — `outbound_composition.py`,
`message_classification.py`, `resolution_state.py`.

### Anchor mismatch found and recorded (not adapted around)

**V1 phase 2 is absent from this base.** `return_case_workflow.py` has no
`support-template-review-gate` patch, no `_open_support` review gate, and no
clarification signal handler; there is no panel module and no section registry.
Greps: `support-template-review-gate|template_review|record_clarification_answer|
relay_clarification_to_support|section_registry|panel` over `src` returns only
prose mentions. The dispatch's premise for item 5 — "implementing the handler
stub V1 declared" — does not hold here, and item 6's seam is confirmed absent.
Reported rather than invented; see the delta report. Nothing in this step
depends on either.

### The two design choices that carry the contract

**Assembly and rung one are one node.** The first draft had an
`assemble_context` node handing its payload to `resolve_from_facts` through the
state — which is every fact value the prompt carries, precisely what
`resolution_state.py` refuses to checkpoint. Collapsed into one node, so the
context never outlives the call; only `consumed_fact_ids` and `content_hash`
(references) are written. Nothing is lost on resume: `assemble_case_context` is
pure, so the same log and policy give back the same bytes and the same hash.

**Conflict is checked before the threshold.** A graph answer that is both
confident and contradicts the case's own record is the most dangerous thing this
ladder can produce — it would go to Support, under the platform's name, against
the record. Checking the threshold first sends exactly that. Fault C proves the
order is load-bearing.

`attempt` is in neither the state nor the thread id. `support_resolver_thread_id`
takes two keyword arguments and there is no third to vary, which is the form of
the acceptance-23 guarantee that survives an edit.

### A real collision, found by my own test, in my own code

The first `support_resolver_thread_id` was a bare f-string join. The collision
test — written with the separator *in the inputs*, per the reviewer standard —
failed immediately: `(case_id="case-1:evt", support_event_id="9")` and
`(case_id="case-1", support_event_id="evt:9")` both minted
`support-resolver:case-1:evt:9`, two support events resuming into one another's
checkpoint. Not hypothetical: `auto_responder.support_event_id_for` mints
`support-response-agent:{work_item_id}` today, which carries a colon. Fixed by
refusing a component that contains the separator, so splitting the id is a total
inverse of building it and the mapping is injective by construction rather than
on the pairs someone thought to try. Fault O reinstates the defect and is caught.

### Fault-injection evidence (18 faults) — two of which my tests were blind to

Baseline `41 passed`. Each applied alone and reverted.

| Fault | Result |
|---|---|
| A a cleared fact rung descends anyway (no short-circuit) | 3 failed |
| B the confidence threshold is ignored | 12 failed |
| C conflict checked *after* the threshold | 1 failed |
| D a `None` agreement is read as a conflict | 8 failed |
| E budget checked after the call instead of before | 1 failed |
| F exhaustion escalates but writes no fact | 1 failed |
| G every escalation reason collapses into one | 4 failed |
| H the question text is passed into the trusted bag | 2 failed |
| I the raw tool read is checkpointed instead of a reference | 1 failed |
| **J `finalize` invents an answer when none cleared** | **41 passed — BLIND** |
| K the gate mode is hardcoded, not read from the release | 1 failed |
| **L the sync node re-syncs on resume** | **41 passed — BLIND** |
| M the tool refusal loses the missing-entity names | 1 failed |
| N the thread id regains an attempt component | 3 failed |
| O a component may carry the separator again | 3 failed |
| P the allowlist is widened to admit `credential` | 2 failed |
| Q an unreadable confidence reads as trusted | 5 failed |
| R the tool runs under the message sender's principal | 1 failed |

**J and L survived the first pass, and for the same reason:** both guard states
the *routers cannot produce*. No whole-graph test can reach `finalize` with
nothing cleared, and LangGraph resumes at the node *after* the last completed
one, so `sync_graph`'s re-entry guard is never exercised from outside. J is the
shape that sends Support a blank message under the platform's name; L is a
duplicated targeted source read. Both are now tested where they live —
`test_finalize_refuses_a_state_in_which_no_rung_cleared` and
`test_the_sync_node_itself_refuses_to_sync_a_second_time` — rather than trusted
from a distance. Re-run: **J → 1 failed, L → 1 failed**, all 18 caught, baseline
`43 passed`.

The lesson is the one step:04 recorded in a different costume: an invariant that
only holds because *callers* are well-behaved is untested unless it is tested
directly, and "no test reaches it" reads identically to "no test can fail on it".

**Tests.** `43 passed` in-slice. `tests/operations tests/platform
tests/configuration` → `1 failed, 1391 passed`, the one failure being the
known pre-existing `test_main_is_composition_only`. Ruff clean on all five files.
**Next:** step:06 — reply gating: `SUPPORT_REPLY` reviews on S2's aggregate with
a server-minted `scope_id`, and the auto_reply path with its disclosure line.

## step:06 — one composed message, two destinations; and condition 10

**New files.** `operations/return_support/reply_gating.py`,
`tests/operations/test_support_reply_gating.py` (13 tests).
**Modified.** `operations/return_support/outbound_composition.py` (the
condition-10 length bound), `operations/fact_names.py` (`SUPPORT_REPLY_DRAFT`).

### The decision that carries brief item 4

`compose_reply` runs **once**, before the gate is consulted, and the resulting
string is what the `SUPPORT_REPLY` review carries as its draft payload *and*
what an `auto_reply` intent posts directly. Composing per path would give the
reviewed message and the unreviewed message two spellings of the disclosure, the
neutralisation and the framing — and the unreviewed one is the path with nobody
looking at it. `test_both_paths_compose_the_identical_message` asserts each
against a **literal**, not against the other: two values from the same function
are equal by construction, and comparing them would pass with both wrong.

The gate **re-reads** `reply_gate` from the release rather than trusting the
ladder's `resolution["requiresReview"]`; the two are written at different times
and the release is the authority (fault C).

`scope_id` is *not* passed to `create_review` — sect. 6 mints a SUPPORT_REPLY
review's scope id server-side — and the value the gate reports is the one that
came back. Both halves are asserted, because either alone is weak: faults D and
E each break exactly one of them.

### Delivery identity without a store

An auto reply has no approving command, so it has no command-derived
`logical_operation_id`. Derived instead: `support-reply:{case_id}:{support_event_id}`
→ `delivery_id = uuid5(fixed namespace, logical_operation_id)`. The observable
property sect. 7 asks for is "a retry reuses the identity and the receiver
absorbs it", and a pure function of two ids gives that without a row that could
be lost between the generate and the store — a stored id has a window where it
is generated and not yet persisted; a derived one has none. Pinned as a literal
uuid, so changing the derivation is a visible edit rather than a silently
different identity (a test asserting only "two calls agree" would pass for a
derivation that returned a constant for every case).

### Carry-forward condition 10, discharged at the seam

A support-derived value reaching associate-facing text with only `.strip()`
behind it is unbounded. `neutralized` now bounds every interpolated value at
`VALUE_CHARACTER_BOUND` (4,000 characters) with a visible `[truncated]` notice.
Two properties, each fault-injected:

* the bound is applied **after** neutralisation, not before — the other order
  lets a value push a framing-shaped line past the cut and out of the
  neutraliser's reach, a truncation that reopens the hole the neutralisation
  closed (fault M);
* the truncation is **visible**, so a reader can tell a cut from a message that
  merely ended (fault N).

The **fact** still stores the value in full: that is the audit record, and
`verbatim_question` means what it says there. Only the rendering is bounded.
Composition emits plain text with no markup, so "render as data, not markup"
holds by construction on this surface; the panel half of it belongs to item 6.

### Condition 7 re-verified on the *finished* reply path

Not inherited from step:04's scope. `test_a_hostile_question_and_a_hostile_answer_
cannot_restructure_the_reply` drives the whole gate with a framing-shaped
support question **and** a framing-shaped model answer, and pins the composed
outbound text as one equality: forged headings are `[removed]`, the real
headings are the module's code constants, and the disclosure is still last.
Fault K reinstates the V1 defect (`neutralized` → `str`) and 16 tests fail.

### Fault-injection evidence (15 faults, all caught, no blind spots)

Baseline `37 passed` (gating + composition). Each applied alone and reverted.

| Fault | Result |
|---|---|
| A the gate sends every reply without review | 4 failed |
| B the gate reviews every reply, ignoring `auto_reply` | 6 failed |
| C the gate trusts the resolution flag, not the release | 6 failed |
| D the gate mints the `scope_id` itself | 2 failed |
| E the gate discards the aggregate's minted `scope_id` | 1 failed |
| F an auto reply posts under an ordinary actor, not SYSTEM | 1 failed |
| G the auto reply drops the disclosure | 6 failed |
| H the delivery id is freshly generated per attempt | 2 failed |
| I the delivery id ignores the support event | 3 failed |
| J the draft fact is written only on the review path | 1 failed |
| K the reply text skips the neutraliser (the V1 defect) | 16 failed |
| L the length bound is removed | 1 failed |
| M the bound is applied before neutralisation | 1 failed |
| N the truncation is silent | 1 failed |
| O a gated reply is delivered as well as reviewed | 1 failed |

(J was first injected with a reference to a function that does not exist, so it
failed for the wrong reason — 11 failures including a `NameError`. Re-injected
honestly as a moved call: **1 failed**, the `auto` parametrization, which is the
assertion that should catch it. Recorded because a fault that fails for the
wrong reason is not evidence.)

**Tests.** `tests/operations tests/configuration` → `1211 passed`, zero new
failures. Ruff clean.
**Next:** step:07 — the clarification round-trip: answer endpoint, command +
outbox, and the two activities.

## step:07 — the clarification round-trip, minus the half this base cannot carry

**New files.** `operations/return_support/clarification.py`,
`api/case_clarifications.py`,
`tests/operations/test_support_clarification_roundtrip.py` (14 tests),
`tests/api/test_case_clarification_answer_route.py` (14 tests).
**Appended.** `operations/fact_names.py` — `SUPPORT_CLARIFICATION_ANSWERED`.

### What was built, and what was not

Built: the answer endpoint (capability + case access + command/outbox in one
transaction), both activity bodies (`record_clarification_answer`,
`relay_clarification_to_support`), the relay's delivery identity, and the
deadline decision.

**Not built, and not worked around:** the `clarification_answered` signal
*handler* on `return_case_workflow.py`, and the reminder-cadence assertion. The
dispatch's premise — "implementing the handler stub V1 declared" — does not hold
on this base: there is no `support-template-review-gate` patch, no `_open_support`
review gate, no `resumed_template_review_deadline_iso`, and therefore no deadline
to reset and nothing to attach a handler to. Writing one would have meant
inventing the workflow contract V1 phase 2 owns, which is exactly what the
dispatch forbids for item 6's seam and is no more permissible here.

What *is* extractable was extracted: `deadline_after_clarification` is the half
of the reset that is a **decision** rather than a mechanism, and it is pure and
tested. When V1 phase 2's gate lands, the handler is three calls — the two
activity bodies and this function.

### One delivery path, not a second one

The relay calls the same `ensure_case_support_thread` + `post_support_message`
pair the reply gate calls. Sect. 7's own words: *"kind-agnostic — both review
kinds use this one receiver-deduped path"*. Each additional posting path is
another chance to get the dedupe identity wrong.

The relay's identity shares `reply_delivery_identity`'s uuid5 namespace, so the
**prefixes** are the only thing keeping the two apart —
`clarification-relay:` vs `support-reply:`. Asserted directly against the same
case id and the same trailing id, so the prefix is the sole difference (fault H
removes it and two tests fail).

### A contract gap found and recorded, not papered over

Sect. 4 says *command-originated facts carry server-stamped `actorId`*. S1's
shipped `append_scoped_case_fact` **has no `actor_id` parameter** — passing one
raises `TypeError` at the real call site. Adding it is a change to a shared S1
signature this slice does not own. The actor is carried in the fact's `value` as
`answeredBy` instead, which preserves the observable property (the answering
principal is on the fact, stamped by the server), and the gap is in the delta
report for the orchestrator.

### Verbatim, neutralised, and bounded — all three, on the relay

Three tests, because two of them alone would be misleading:

* the **fact** keeps the answer exactly as typed, framing-shaped lines and all
  (fault D breaks this and is caught) — the audit record must not disagree with
  what the associate wrote;
* the **relay** neutralises both halves and is pinned as a whole-output equality
  against a hostile question *and* a hostile answer (condition 7);
* an **ordinary** question mentioning an RMA, a bay and a colon passes through
  byte for byte, also as a whole-output equality — which is what keeps
  "verbatim" an honest word rather than a claim;
* an enormous answer is bounded with a visible notice (condition 10).

### Fault-injection evidence (17 faults, all caught)

Baseline `28 passed`. Each applied alone and reverted.

| Fault | Result |
|---|---|
| A the answer fact is INFERRED, not STATED | 1 failed |
| B the answer fact is filed on Channel B | 1 failed |
| C the fact ignores the record the answer mapped to | 1 failed |
| D the fact neutralises the answer (audit ≠ what was typed) | 1 failed |
| E the relay drops the question and sends only the answer | 3 failed |
| F the relay drops the disclosure | 2 failed |
| G the relay delivery id is generated per attempt | 2 failed |
| H the relay identity loses its prefix (collides with a reply) | 2 failed |
| I the answer text is mirrored into the business payload | 1 failed |
| J the deadline reset ignores the released switch | 1 failed |
| K a reset can move the deadline inwards | 1 failed |
| L the signal id is random (double submit = two commands) | 4 failed |
| M the endpoint accepts a `map` with no record | 1 failed |
| N the endpoint accepts a body field it does not know | 1 failed |
| O the endpoint stamps something other than the authenticated actor | 1 failed |
| P a wrong-tenant case is a 403 that confirms it exists | 2 failed |
| Q a conflicting second answer is swallowed as accepted | 1 failed |

(Q was first injected as an `except` clause that still produced the 409 — a
no-op that reported `28 passed` and would have read as a blind spot. Re-injected
as a genuine swallow: **1 failed**. Recorded, because a fault that does not
change behaviour is not evidence either way.)

**Tests.** `tests/operations tests/api tests/configuration` → `1548 passed`,
zero failures, zero new. Ruff clean.
**Next:** step:08 — item 6's seam check, integration lines, and the delta report.
