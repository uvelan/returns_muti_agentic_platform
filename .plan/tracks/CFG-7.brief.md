# CFG-7 — Acceptance and hardening: every screen proven live, every carried advisory closed, CI truthful

Base: the trunk after CFG-5b merges. Branch `feat/cfg-7-acceptance`. Worktree `.claude/worktrees/cfg-7` (venv + node_modules junctioned, `.env` copied, `PYTHONPATH` pinned and printed in step:00). Models: Haiku scanners for the sweeps and mock catalogues, Sonnet for fixes, Opus RV. Read first: `.plan/tracks/CFG.brief.md` §6 (definition of done), every `.plan/reviews/CFG-*.md` (the carried advisories below are drawn from them), `scripts/ci/known_test_failures.json` and `scripts/ci/assert_known_failures.py`, `frontend/e2e/**`, `frontend/playwright.config.*`.

Owns: `frontend/e2e/**`, `frontend/src/domains/config/**` (advisory fixes only), `frontend/src/components/forms/**` (advisory fixes only), `frontend/src/mocks/**`, `scripts/ci/**`, `docs/configuration/**`, `docs/README.md` and the docs that name configuration files, `.plan/acceptance/config-screens.md` (new), `backend/tests/**` only to register or fix the pre-existing failures named below, `docs/evidence/stage4o_complete_audit/generate_audit_artifacts.py` (the two deleted filenames). Must not touch: production backend code except a fix a named pre-existing failure requires (record it), the bootstrap, the release API.

Budget: 400k. Stop rule: `drop.json` PARTIAL at 320k.

## Scope
1. **Live acceptance loop over every configuration screen** (`/config/overview`, `discovery`, `policy`, `return-policy`, `fulfilment`, `workflow`, `support` (six tabs), `integrations`, `agents` (proposal path), `simulation`, `source-bindings`, `deployment`, and AI Control Center Configuration and Providers): one Playwright spec each, run in one pass with `--workers=1` against a disposable dev server over the live backend; each spec changes one value, validates, publishes (or proposes and activates), asserts `GET /api/config/runtime`, and publishes the revert. Record the run in `.plan/acceptance/config-screens.md` with head revisions before/after and a per-screen row (value changed, runtime observed, reverted, audit record id).
2. **Axe sweep** of every `/config/*` route and `/ai/configuration`, `/ai/providers-models` in the mock server: zero violations; fix what fails (CFG-8 A2 `disabled:opacity-40` on text at `PolicySection.tsx`; CFG-5 H1/H2 disabled-control styling and the opacity-spelling test; the `/config/return-policy` reflow at 320px, CFG-5 H5).
3. **Carried advisories**, each closed with a test or a ledger reason: CFG-8 A1 (pin `FERGUSON_STANDARD_RETURN` last in precedence; map the model-level error path to the list), A3 (move the preview panel outside the read-only fieldset so a read-only operator can Evaluate), forward note (the policy e2e's APPROVE depends on the live `policy_evaluation.enabled`; make the spec assert its precondition or set it explicitly); CFG-5 H4 (the missing rationale comment at `DataSourcesSection.tsx:400`); CFG-6 A6 (HTTP-layer tests for the production gate 422 and the startup refusal), A7 (`EnumSelect` disabled-option tests), A8 (axe for `/config/deployment`), A11 (document the whole-`deployment` production gate on every RETURN_PLATFORM publish); CFG-3b A2/A5–A8 not already closed; CFG-4 F6/F10 as recorded.
4. **Mock layer complete**: handlers for the routes the "warn" setting surfaced (`/api/config/adoption`, `/api/shipment-status-catalog`, `/api/shipments`, the two `/support/*`), contract tests, `npm run dev:mock` sweep clean with `onUnhandledRequest: "error"` restored.
5. **CI truthful**: the 42 backend failures that pre-exist on the trunk (ids in `.plan/reviews/CFG-1.md`) are either fixed (the configuration-adjacent ones) or registered in `scripts/ci/known_test_failures.json` with the module and reason, so `assert_known_failures.py` passes on the trunk; the frontend list stays empty; `suite_size_floor.json` updated to the new counts.
6. **Docs**: `docs/configuration/families.md`, both configuration READMEs, `docs/README.md` links, `.env.example` comments: describe the split directories, the release domains, the `deployment` section, the undecided-keys workflow (`--adopt-packaged-key` and the Overview panel), and the screens; `docs/evidence/stage4o_complete_audit/generate_audit_artifacts.py:264,266` no longer names deleted files.
7. **Final audit delta**: append a section L to `evidence/config_audit/FINAL_REPORT.md` listing each §G defect's final status with the lease that closed it and the evidence pointer, and re-render the report artifact source.

## Acceptance
- `.plan/acceptance/config-screens.md` shows every screen green in one live pass, values reverted, head revisions and audit ids recorded.
- Axe: zero violations across the swept routes; `npx vitest run` 0 failures; typecheck; lint; backend `pytest tests -q --ignore=tests/configuration/test_concurrent_activation.py` with `assert_known_failures.py` PASS on the trunk; drift check PASS; `dev:mock` sweep clean under `"error"`.
- Definition of done (CFG.brief.md §6) checked item by item in the ledger with pointers.
