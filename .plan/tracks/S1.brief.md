# S1 — Model & identity (binding-only, per DR-11)

Self-contained brief. Read `.plan/contracts.md` first — §4 is your contract; §1 DR-11 is your scope ruling. Branch `feat/s1-model-identity` off `.plan/base.sha`. Ledger: `.plan/tracks/S1.ledger.md` (append-only, one entry per step). Commit per step: `(S1) step:<id> …`.

## Scope

DR-11 deleted the formation/reconciliation/migration programme. S1 delivers exactly:

1. **Fact scoping.** Add `record_scope: str | None = None` to `CaseFactView` (`backend/src/return_platform/operations/models.py`) — additive, defaulted null (existing facts remain valid case-level facts). Add `identity_version: int` stamping for the new path only.
2. **`append_scoped_fact_once`** — a NEW method beside (never replacing) `_append_fact_once` in `backend/src/return_platform/workflows/return_case_activities.py` and/or the repository layer (follow where `_append_fact_once` lives; anchor: `return_case_activities.py:485`). Its fact-identity derivation includes `record_scope`; it stamps `identity_version`; legacy algorithm untouched. Replay test required: a fact written via the legacy path pre-deploy, its event retried post-deploy through the new code → no duplicate.
3. **Projection `latest_case_facts_scoped(case_id)`** in `backend/src/return_platform/operations/repository.py` (anchor: existing `latest_case_facts`) — latest per `(record_scope, factName)`. Do NOT modify `latest_case_facts` or its 11 consumers (enumerated in contracts.md §2.4).
4. **`operations/fact_names.py`** (new) — constants for exactly the names in contracts.md §4, **only the ones S1/S2 code actually references now**; no unused constants (RV greps). Later slices add their own constants when they first reference them.
5. **Enum members** `ASSOCIATE_EDIT`, `CONTEXT_SUMMARY` on the acquisition-method enum in `operations/models.py` (anchor: `FactAcquisition`), plus validator/projection acceptance.
6. **Loose-artifact binding module** (new `backend/src/return_platform/operations/artifact_binding.py`) — S1 is the SOLE owner; V2 will consume it and may not edit it. Pure function + persistence helper implementing contracts.md §4 binding rules over `ExtractedArtifact{artifact_type: RMA|TRACKING|LABEL|SHIPPING_INSTRUCTION|RETURN_LOCATION, value, binding}` with statuses `BOUND|AMBIGUOUS|UNMATCHED`. Bound artifacts merge into the record via the existing `RETURN_RECORD_MERGED_FIELDS` upsert semantics (anchor: `return_case_activities.py:202`, `_plan_for`/`_apply_record` ~1952–2018); AMBIGUOUS/UNMATCHED write scoped facts (`support_artifact_ambiguous`/`support_artifact_unmatched`) and never touch a record.

## Anchors (verify before writing; mismatch = halt, report in ledger)

- `CaseFactView` in `operations/models.py`; `FactAcquisition` enum (STATED/OBSERVED/DERIVED/INFERRED).
- `_append_fact_once` at `workflows/return_case_activities.py:485` (unique `factId`, swallows DuplicateKeyError).
- `latest_case_facts` in `operations/repository.py`.
- `RETURN_RECORD_MERGED_FIELDS` at `return_case_activities.py:202`; `_plan_support_outcome` docstring ~1931 ("the RMA identifies the thing Support issued").
- Records keyed `(caseId, returnReference)` unique partial index; `returnRecordId` stable.

## Not in scope

No formation rule, no reconciliation ops, no resolution table, no migration, no `record_model_version`. No workflow-file edits. No API. No frontend. No shared-file edits (`return_configuration.py`, `production.yaml`, `ai_gateway.yaml`) — none should be needed; if one appears needed, halt.

## Definition of done

- Unit tests beside the code (normal suite, no live infra): scoped append-once identity (two records, same fact name → two facts); legacy replay no-dup; scoped projection partitioning; every binding-rule branch (known ref → bind; unknown ref → UNMATCHED fact, no record; no ref + 1 record → bind; no ref + N records → AMBIGUOUS fact); merged-fields null-never-overwrites preserved.
- `python -m pytest backend/tests` green (default deselection).
- Ledger complete; nothing uncommitted; delta report appended to ledger (files touched, contract points consumed, deviations, open risks).
