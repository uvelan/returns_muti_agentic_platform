# S1 ledger — model & identity (binding-only, per DR-11)

Append-only. One entry per step. Branch `feat/s1-model-identity` off `a50c5500788f99e909f23099a81731b37c736b8c`.

---

## step:01 — branch + anchor verification

- **Files touched:** `.plan/tracks/S1.ledger.md` (new; this file).
- **Commands run:**
  - `git checkout -b feat/s1-model-identity a50c5500788f99e909f23099a81731b37c736b8c` → OK.
  - Test harness probe: worktree has no venv; main checkout's `backend/.venv` has the deps with an editable install pointing at the main checkout's `src`. Verified `PYTHONPATH=<worktree>/backend/src` makes the worktree source win (`case_repository.__file__` resolves into the worktree). Copied the untracked, gitignored root `.env` from the main checkout into the worktree root (required by `tests/conftest.py::pytest_configure`; never committed).
  - `python -m pytest tests -q --collect-only` (from `backend/`, with the PYTHONPATH above) → `4354/4865 tests collected (511 deselected)` — default deselection (`not live_infra and not browser`) active.
- **Anchors verified (all against branch tip = base.sha):**
  - `CaseFactView` — `operations/models.py:204`, MutableContract (pydantic, `extra="forbid"`), camelCase stored keys, append-only docstring. ✓
  - `FactAcquisition` — `operations/models.py:157`, StrEnum with exactly STATED/OBSERVED/DERIVED/INFERRED. ✓
  - `_append_fact_once` — `workflows/return_case_activities.py:485`, exactly as described (unique `factId`, absorbs `DuplicateKeyError`, returns wrote/already-recorded bool). ✓
  - `RETURN_RECORD_MERGED_FIELDS` — `return_case_activities.py:202`, tuple of `(stored_key, attribute, fact_name)`; merged-never-replaced, None never overwrites (docstring 186–201, `_plan_for` 1952–1973). ✓ (Note: base.sha also carries a sixth row `carrier` beyond the four the brief names; additive, consistent with the anchor.)
  - `_plan_support_outcome` docstring — `return_case_activities.py:1931-1938`, verbatim "the workflow's minted id identifies an *attempt*, while the RMA identifies the thing Support issued". ✓ `_plan_for`/`_apply_record` at 1952–2018. ✓
  - Records keyed `(caseId, returnReference)` unique partial index — `case_repository.py:426-433` (`create_return_record` docstring names the index; replay-safe no-op). `returnRecordId` stable (existing record's id reused, `return_case_activities.py:1968`). ✓
  - `latest_case_facts` — **anchor location note, not a mismatch:** the brief says "in `operations/repository.py`"; the method is *defined* on `CaseRepository` in `operations/case_repository.py:388` and is exposed on `OperationalRepository` (`operations/repository.py:158`, `class OperationalRepository(CaseRepository)`) by inheritance — `repository.py` itself defines no fact methods. The anchor ("existing `latest_case_facts`") exists and matches the described semantics (latest per factName, `(recordedAt, factId)` tie-break). Resolution: `latest_case_facts_scoped` and the scoped append persistence go on `CaseRepository` in `case_repository.py`, directly beside the methods they mirror — the same "follow where the anchor lives" rule the brief itself gives for `_append_fact_once` — and are therefore available on `OperationalRepository` in `repository.py` in exactly the sense the existing anchor is. Recorded here for RV; no scope widened, `latest_case_facts` itself untouched.
- **Test result:** collection-only smoke green (no code changes yet).
- **Next step:** step:02 — `CaseFactView.record_scope`/`identity_version` + `FactAcquisition.ASSOCIATE_EDIT`/`CONTEXT_SUMMARY` in `operations/models.py`, with unit tests.

## step:02 — fact-model scoping fields + acquisition-method additions

- **Files touched:** `backend/src/return_platform/operations/models.py` (additive: `CaseFactView.record_scope: str | None = None`, `CaseFactView.identity_version: int | None = None`, `FactAcquisition.ASSOCIATE_EDIT`, `FactAcquisition.CONTEXT_SUMMARY`); `backend/tests/operations/test_scoped_case_facts.py` (new).
- **Contract points:** §4 "record_scope … additive field on CaseFactView", "AcquisitionMethod additions"; brief items 1 and 5. Field names taken verbatim from the brief (`record_scope`, `identity_version`) — snake_case beside the file's camelCase stored keys, deliberate: the contract names these exact spellings, and the stored key equals the field name so pre-S1 documents validate with `None` defaults under `extra="forbid"`.
- **Validator/projection acceptance:** checked — no closed-set validator constrains `acquisitionMethod` anywhere (`case_projection/assembly.py:495` passes text through; StrEnum membership is the only gate), so the two enum members are the entire change.
- **Commands run / test result:** `pytest tests/operations/test_scoped_case_facts.py -q` → **5 passed**.
- **Anchors verified:** `CaseFactView` (models.py:204), `FactAcquisition` (models.py:157) — re-confirmed pre-edit this step.
- **Next step:** step:03 — `append_scoped_case_fact` + `latest_case_facts_scoped` on `CaseRepository`, `append_scoped_fact_once` beside `_append_fact_once` in `return_case_activities.py`, with identity/replay/projection unit tests.

## step:03 — scoped append path + scoped projection

- **Files touched:** `backend/src/return_platform/operations/case_repository.py` (new `append_scoped_case_fact` beside `append_case_fact`; new `latest_case_facts_scoped` beside `latest_case_facts` — both legacy methods byte-untouched); `backend/src/return_platform/workflows/return_case_activities.py` (new module constant `SCOPED_FACT_IDENTITY_VERSION = 2`, new `append_scoped_fact_once` beside `_append_fact_once` — legacy method byte-untouched); `backend/tests/operations/test_scoped_case_facts.py` (extended).
- **Contract points:** §4 fact scoping — identity includes `record_scope` (`{fact_id}::{record_scope}` when scoped, the caller's legacy-derived id verbatim when scope is `None` — this is the replay guarantee), stamps `identity_version`; projection partitions by `(record_scope, factName)`; legacy `_append_fact_once` and `latest_case_facts` untouched (11 consumers unmodified — convergence stays the §10 follow-up). Brief items 2 and 3.
- **Placement:** per step:01's anchor note, both repository methods live on `CaseRepository` (`case_repository.py`) beside the methods they mirror, and are therefore exposed on `OperationalRepository` (`operations/repository.py`) by inheritance exactly as `latest_case_facts` is.
- **Tests (all fake-backed unit, real `CaseRepository`/`ReturnCaseActivities` code under test):** two records same fact name → two facts, distinct ids, scope + identity_version stamped; scoped retry absorbed; **legacy replay no-dup** (pre-deploy `_append_fact_once` write, post-deploy retry through `append_scoped_fact_once(record_scope=None)` → absorbed, legacy document shape untouched); scoped write bumps case revision (plan §6.5); scoped projection partitions `(None|record-1|record-2, name)`; newest-wins within a partition under a ticking clock; `latest_case_facts` shape unchanged by scoped writes.
- **Commands run / test result:** `pytest tests/operations/test_scoped_case_facts.py tests/test_cumulative_support_outcomes.py -q` → **62 passed, 1 failed**; the failure (`test_a_rejected_return_still_opens_no_work_item`, `AttributeError: '_Runtime' object has no attribute 'patched'`) **pre-exists at base.sha** — verified by `git stash` → same failure on the untouched tree → `git stash pop`. Environment drift (temporalio in the shared venv), not S1. Open risk noted for the delta report.
- **Anchors verified:** `_append_fact_once` (return_case_activities.py:485 pre-edit), `latest_case_facts`/`append_case_fact` (case_repository.py:388/328 pre-edit).
- **Next step:** step:04 — `operations/fact_names.py` + `operations/artifact_binding.py` (binding rules, merged-fields persistence, scoped-fact writes) with branch-complete unit tests.

## step:04 — fact-name constants + loose-artifact binding module

- **Files touched:** `backend/src/return_platform/operations/fact_names.py` (new — exactly two constants, `SUPPORT_ARTIFACT_AMBIGUOUS` and `SUPPORT_ARTIFACT_UNMATCHED`, the only §4 names S1 code references; no unused constants ship, later slices append their own); `backend/src/return_platform/operations/artifact_binding.py` (new — S1 sole owner, V2 consumes); `backend/tests/operations/test_artifact_binding.py` (new).
- **Contract points:** §4 binding rules verbatim, in code and never a prompt: known reference → BOUND; unknown reference → UNMATCHED, never a new record (the persistence port deliberately has no create method at all); no reference + one record → BOUND; no reference + several → AMBIGUOUS. Statuses `BOUND|AMBIGUOUS|UNMATCHED`. Bound artifacts merge under `RETURN_RECORD_MERGED_FIELDS` semantics (only the artifact's field written, redelivery writes nothing, one re-read retry on version conflict, second conflict propagates — `_update_record_once_retried`'s shape). AMBIGUOUS/UNMATCHED write their scoped facts through the `append_scoped_fact_once` port (case-level scope, deduped on the caller's `dedupe_key`) and never touch a record. Brief items 4 and 6.
- **Recorded interpretations (not deviations):** (a) the contract leaves "no reference + zero records" open; closed conservatively as UNMATCHED (binding and disambiguation both impossible, record creation forbidden) — documented in module + test. (b) `ARTIFACT_STORED_FIELDS` mirrors `RETURN_RECORD_MERGED_FIELDS`' stored keys instead of importing them (the import would point operations→workflows, against the dependency direction V2 will need); a pin test (`TestStoredFieldMirror`) fails on any drift. (c) An RMA-type artifact names itself (`value` is a return reference by definition) and, when bound, merges nothing — identity confirmation, not data.
- **Commands run / test result:** `pytest tests/operations/test_artifact_binding.py tests/operations/test_scoped_case_facts.py -q` → **30 passed**. RV-style grep: no literal of either fact name outside `fact_names.py` (src + tests).
- **Anchors verified:** `RETURN_RECORD_MERGED_FIELDS` (return_case_activities.py:202), `_plan_for`/`_update_record_once_retried` semantics (1952–2059) — re-read this step before mirroring.
- **Next step:** step:05 — full default-deselection suite `python -m pytest tests` from `backend/`; delta report.

## step:05 — full suite + delta report (slice complete)

- **Files touched:** `.plan/tracks/S1.ledger.md` only.
- **Commands run / test result:** `python -m pytest tests -q` from `backend/` (default deselection) → **2 failed, 4373 passed, 9 skipped, 511 deselected in 173s**. Both failures verified **pre-existing at base.sha** by running them on a detached checkout of `a50c550` in this worktree (clean tree) → same 2 failures there; branch restored to `feat/s1-model-identity` afterwards:
  - `tests/test_cumulative_support_outcomes.py::test_a_rejected_return_still_opens_no_work_item` — `AttributeError: '_Runtime' object has no attribute 'patched'` (temporalio drift in the shared venv; `workflow.patched` outside a workflow sandbox).
  - `tests/platform/test_main_is_composition_only.py::test_router_mounting_is_the_bulk_of_create_app_and_that_is_allowed` — fails identically at base.sha.
  Every test S1 added or that touches S1 code passes; the S1 diff introduces **zero new failures** (4373 passed = base's passing set + S1's 30 new tests).
- **Incident, recorded:** while re-verifying, a `git stash` on an already-clean tree saved nothing, so the subsequent `git stash pop` applied the worktree's **pre-existing** stash entry (`stash@{0}: On master: graph-schema-analyzer-from-master-before-unified-port`), producing merge conflicts. Recovered with `git reset --hard HEAD`; tree verified clean at `305e794`; the stash entry itself is **preserved untouched** in the stash list. No S1 commit or file was affected.

---

## Delta report

- **Branch:** `feat/s1-model-identity` off `a50c5500788f99e909f23099a81731b37c736b8c` (= `.plan/base.sha`). Purely additive diff: 8 files, +1105/-0.
- **Files touched (entire owned set, nothing outside it):**
  - `backend/src/return_platform/operations/models.py` — `CaseFactView.record_scope`, `CaseFactView.identity_version` (both defaulted `None`; pre-S1 documents validate unchanged under `extra="forbid"`); `FactAcquisition.ASSOCIATE_EDIT`, `FactAcquisition.CONTEXT_SUMMARY`.
  - `backend/src/return_platform/operations/case_repository.py` — new `append_scoped_case_fact` and `latest_case_facts_scoped`, each beside its legacy sibling; `append_case_fact` / `latest_case_facts` byte-untouched.
  - `backend/src/return_platform/workflows/return_case_activities.py` — new `SCOPED_FACT_IDENTITY_VERSION = 2` and `append_scoped_fact_once` beside `_append_fact_once` (byte-untouched). No workflow-definition file edited.
  - `backend/src/return_platform/operations/fact_names.py` (new) — `SUPPORT_ARTIFACT_AMBIGUOUS`, `SUPPORT_ARTIFACT_UNMATCHED`; nothing unused.
  - `backend/src/return_platform/operations/artifact_binding.py` (new, S1 sole owner) — pure `bind_artifact`/`bind_artifacts` + `persist_binding_decision` over ports.
  - `backend/tests/operations/test_scoped_case_facts.py`, `backend/tests/operations/test_artifact_binding.py` (new; 30 tests).
  - `.plan/tracks/S1.ledger.md` (this file).
- **Contract points consumed:** contracts.md §4 in full (record creation untouched per DR-11; binding rules; fact scoping; the two fact-name constants S1 references; AcquisitionMethod additions); §1 DR-11 (scope ruling — no formation/reconciliation/resolution/migration shipped); §2.1/§2.4 (identity + consumer freeze honored); §3 process rules.
- **Deviations:** none from contract semantics. Three ledger-recorded interpretations: (1) scoped repository methods live on `CaseRepository` in `case_repository.py` — where the brief's anchor `latest_case_facts` is actually defined — and reach `operations/repository.py`'s `OperationalRepository` by inheritance (step:01 anchor note); (2) "no reference + zero records" (a case the contract's table doesn't cover) closed as UNMATCHED, never a created record (step:04); (3) `ARTIFACT_STORED_FIELDS` mirrors rather than imports `RETURN_RECORD_MERGED_FIELDS` stored keys, pinned by a drift test (step:04).
- **Open risks:**
  - Two pre-existing base.sha failures (step:05) — environment drift, outside S1 scope; flagged for the orchestrator/RV so later slices don't mis-attribute them.
  - The `::`-separator identity derivation assumes no legacy `fact_id` ends in `::<returnRecordId>`; none does today (legacy ids are `name-case-suffix` shaped), and `identity_version` stamps make the derivation auditable if that ever changes.
  - `server-stamped actorId` for command-originated facts (contracts §4 last line) has no S1 surface — no command path exists yet; lands with the slices that own commands (S2/V-slices).
  - Test harness note for successors: worktree has no venv; run with `PYTHONPATH=<worktree>/backend/src` and the main checkout's `backend/.venv` python, and the root `.env` must exist in the worktree (copied, gitignored).
- **Definition of done:** unit tests beside the code for every named behavior — present and green; full default-deselection suite — green modulo the two pre-existing base.sha failures verified above; ledger complete; nothing uncommitted.

---

# S1 ledger — phase 1b: server-stamped `actorId` on the fact plane

Append-only, continuing above. Branch `feat/s1-actor-id` off the current head of
`refactor/unified-return-platform` (`3d8715f`, `(RV) review:V3-1`). Additive-only
cross-slice fix closing the phase-1 open risk recorded four lines above: contracts §4's
last sentence — *"Command-originated facts carry server-stamped `actorId`"* — is
unimplementable at trunk, and two slices have now had to work around it.

## step:01b-01 — branch, anchor verification, and before-evidence for the defect

- **Files touched:** `.plan/tracks/S1.ledger.md` (this entry).
- **Branch-point note (recorded, not a mismatch):** the dispatch named trunk head
  `24e01b1`; the actual head of `refactor/unified-return-platform` at branch time is
  `3d8715f`, four commits later (`24e01b1` verified an ancestor via
  `git merge-base --is-ancestor` → true). The dispatch says "the current head of
  `refactor/unified-return-platform`", so the branch follows the instruction rather than
  the stale sha. Trunk additionally carries the `(merge-state)` V3-under-review commits
  and `8c71642` (AMENDMENT-3). No anchor S1 owns moved.
- **Anchors verified (all against branch tip `3d8715f`, pre-edit):**
  - `CaseFactView` — `operations/models.py:219`, `MutableContract`, camelCase stored keys,
    carrying S1 phase 1's `record_scope` (248) and `identity_version` (253). ✓
  - `append_case_fact` — `case_repository.py:328` (legacy; document literal 359–373). ✓
  - `append_scoped_case_fact` — `case_repository.py:383` (document literal 411–428,
    required kwargs `record_scope`, `identity_version`). ✓
  - `latest_case_facts` — `case_repository.py:447` (legacy, per-`factName`). ✓
  - `latest_case_facts_scoped` — `case_repository.py:469`, partitions
    `(record_scope, factName)`, returns whole stored documents. ✓
  - `_append_fact_once` — `return_case_activities.py:493` (legacy). ✓
  - `append_scoped_fact_once` — `return_case_activities.py:531`; identity derivation is
    exactly `derived_id = fact.pop("fact_id")` then `f"{derived_id}::{record_scope}"`
    when scoped. ✓
  - `SCOPED_FACT_IDENTITY_VERSION = 2` — `return_case_activities.py:458`. ✓
  - contracts.md §4 final bullet — *"AcquisitionMethod additions: `ASSOCIATE_EDIT`,
    `CONTEXT_SUMMARY`. Command-originated facts carry server-stamped `actorId`."* ✓
    (`actorId`, camelCase, is the contract's own spelling — the stored field name follows
    it, and matches every other stored key on the document.)
- **Before-evidence for the defect (run against the untouched branch tip):**
  ```
  append_scoped_case_fact accepts actor_id: False
  append_scoped_fact_once params: ['self', 'record_scope', 'fact']
  CaseFactView has actorId field: False
  CaseFactView fields: ['acquisitionMethod', 'agentId', 'caseId', 'channel',
    'correlationId', 'factId', 'factName', 'identity_version', 'observedAt',
    'record_scope', 'recordedAt', 'sourcePath', 'sourceSystem', 'supersedesFactId',
    'turnId', 'value']
  ```
  Both halves of the reported gap confirmed: no parameter, and no field for it to land in.
  (`append_scoped_fact_once` swallows `actor_id=` into `**fact` and raises `TypeError`
  one frame down, in `append_scoped_case_fact`.)
- **Scope finding recorded now, carried to the delta report — the REST fact projection:**
  `operations/case_projection/contract.py:154 CaseFactProjection` is an **explicit
  allowlist** of provenance fields (`factId, factName, value, agentId, channel,
  sourceSystem, acquisitionMethod, observedAt, recordedAt, supersedesFactId`), populated
  field-by-field by `case_projection/assembly.py:472 project_facts` from
  `latest_case_facts`. It already drops `record_scope`, and it would drop `actorId`. It is
  the one place in the tree where "written but not readable" is real rather than
  theoretical. **Deliberately not changed here**, because it is published: verified
  `CaseFactProjection` appears in the committed `openapi.json`, and
  `tests/test_openapi_contract_drift.py` pins **four** committed JSON snapshots plus
  `frontend/src/api/generated/return-platform.d.ts` against the generator. Adding a field
  to it therefore requires regenerating five shared artifacts — which contracts §3 assigns
  to the **integration agent** ("OpenAPI export") inside the slice integration branch, and
  which this dispatch fences off ("do not … edit shared config files"). Baseline recorded
  so a successor can tell drift from breakage: `pytest tests/test_openapi_contract_drift.py
  -q` → **5 passed, 1 skipped** (the skip is the TypeScript stage, `TS_GEN_UNAVAILABLE` —
  no `node_modules` in a worktree — which skips *with a reason* by design). Named as a
  dispatch item in the delta report rather than silently taken or silently dropped.
- **Test harness:** as phase 1 recorded — main checkout's `backend/.venv` python,
  `PYTHONPATH=<worktree>/backend/src` (verified: `case_repository.__file__` resolves into
  this worktree), root `.env` copied in (gitignored, never committed). The runner script
  lives outside the worktree so the tree stays clean.
- **Test result:** no code changed this step. Baselines taken:
  `tests/operations/test_scoped_case_facts.py -q` → **12 passed**;
  `tests/test_openapi_contract_drift.py -q` → **5 passed, 1 skipped**.
- **Next step:** step:01b-02 — `CaseFactView.actorId` in `operations/models.py`, defaulted
  `None`, with the pinned-key-set test *extended* (never weakened) and the missing-default
  fault injected and recorded.

## step:01b-02 — the field, the parameter, and the tests that would notice their absence

- **Files touched:**
  - `backend/src/return_platform/operations/models.py` — `CaseFactView.actorId: str | None
    = None`, placed with the provenance fields it belongs to (directly after `agentId`,
    which it is deliberately *not* a synonym for: `agentId` is which software wrote the
    fact, `actorId` is on whose authority). Defaulted, so no stored document is required
    to gain a key.
  - `backend/src/return_platform/operations/case_repository.py` — `append_scoped_case_fact`
    gains `actor_id: str | None = None` and stores it as `"actorId"`. Written on every
    scoped write, `None` included, so a reader can tell *no actor* from *field absent*.
    Docstring states why it is not part of the identity.
  - `backend/src/return_platform/workflows/return_case_activities.py` —
    `append_scoped_fact_once` gains `actor_id: str | None = None` and passes it through.
    The derivation two lines below is untouched and the docstring says so explicitly.
  - `backend/tests/operations/test_case_fact_actor.py` (new, 17 tests).
  - `backend/tests/operations/test_scoped_case_facts.py` — the phase-1 pinned-key-set test
    `test_a_pre_deploy_fact_validates_as_a_case_level_fact` **extended** with
    `assert validated.actorId is None`. The `_stored_fact` pin itself is byte-untouched:
    the new field joins the additive rule, it is not exempted from it.
- **Contract points:** §4 final bullet, *"Command-originated facts carry server-stamped
  `actorId`"* — the stored key is the contract's own camelCase spelling, which is also
  every other stored key's convention on this document (the phase-1 `record_scope` /
  `identity_version` snake_case is the exception, and it exists because the brief named
  those spellings verbatim; nothing here changes them). Parameter spelling `actor_id`
  follows the file's snake_case keyword convention, as `record_scope` does.
- **Spellings this does *not* introduce:** no value-level `answeredBy` (V3) and no
  value-level `actorId` (V1 phase 2) is read, written, or migrated here. Those are their
  slices' to retire now that a top-level field exists; the delta report names exactly what
  each must change.
- **Test design, against the known blind shapes:** the pin `LEGACY_STORED_KEYS` is a
  hand-written literal, not `set(append_case_fact(...))` — a pin derived from the code it
  pins agrees by construction and would keep agreeing while both drifted; the shipped
  legacy writer is then made to answer to it by exact set equality. Identity is asserted by
  **stating the whole `factId`** (`"support_template_revision-case-1-evt-1::record-1"`)
  rather than by "does not contain the actor", which would pass against a derivation that
  hashed the actor in. Actor values are asserted at their exact full value, never by
  prefix. `test_the_model_did_not_get_loosened_to_make_room` asserts `extra="forbid"` still
  raises, because loosening the model is the way every other assertion in the file could go
  vacuous. And `test_no_stored_key_is_required_that_was_not_required_before` compares the
  model's *required-field set* to an exact literal, so a missing default fails by naming
  itself rather than by a distant validation error.
- **Legacy-path byte-identity, proved rather than asserted:** a script extracts the exact
  source text of `append_case_fact`, `latest_case_facts`, `list_case_facts` and
  `_append_fact_once` from the working tree and from `refactor/unified-return-platform` via
  `git show`, and compares bytes:
  ```
  IDENTICAL  append_case_fact  (2157 chars)
  IDENTICAL  latest_case_facts  (990 chars)
  IDENTICAL  list_case_facts  (299 chars)
  IDENTICAL  _append_fact_once  (1950 chars)
  ```
  `git diff -U0` corroborates: every hunk in both source files falls inside the *scoped*
  members (`case_repository.py` @@395, @@410, @@446; `return_case_activities.py` @@531,
  @@552, @@572), none inside the legacy ones.
- **Commands run / test result:**
  - `pytest tests/operations/test_case_fact_actor.py tests/operations/test_scoped_case_facts.py -q`
    → **29 passed**.
  - `pytest tests/operations tests/platform -q` → **1 failed, 1070 passed, 155 deselected**.
    The one failure is the known pre-existing `test_main_is_composition_only` (35 mounts vs
    an expected 33 — two undocumented pre-existing mounts, `shipment_console_router` and
    `template_preview_router`, neither S1's). Zero new failures.
  - `ruff check` over all five touched files → **All checks passed!**
- **Anchors verified (re-read pre-edit this step):** `CaseFactView` (models.py:219),
  `append_scoped_case_fact` (case_repository.py:383), `append_scoped_fact_once`
  (return_case_activities.py:531), `SCOPED_FACT_IDENTITY_VERSION` (458).
- **Next step:** step:01b-03 — inject all four faults, one at a time, and record before/after.
