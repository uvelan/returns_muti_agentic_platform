# V1 ledger — phase 1 (brief items 1, 2, 5)

Append-only. Branch `feat/v1-template-review` off `7438e07` (S1 candidate head).
Phase 1 scope: template config model + formatter allowlist + production.yaml block;
renderer; template config UI + variant preview endpoint. Items 3–4, 6–8 are phase 2.

## step:01 — branch + anchors + ledger

- Files: `.plan/tracks/V1.ledger.md` (this file).
- Commands: `git checkout -b feat/v1-template-review 7438e07084c33b92971671cd821db8424b828778`.
- Anchors verified:
  - `OnDemandSyncCoordinator` at `dynamic_knowledge/on_demand_sync/coordinator.py:94`, `synchronize` at :120 (kwargs: schema, graph_generation_id, request_digest, plan, origin).
  - `latest_case_facts_scoped` at `operations/case_repository.py:469` (S1) — returns `dict[(record_scope|None, factName) -> fact document]`.
  - Record projection: `project_return_record` / `project_return_records` at `operations/case_projection/assembly.py:792/820`; `ReturnRecordProjection` at `case_projection/contract.py:394` (attrs: returnRecordId, returnReference, status, returnMethod, returnLocation, approvedItems, shipments, artifacts).
  - `DocumentEditor` in `frontend/src/domains/config/AgentsSection.tsx:205/215`.
  - `compose_support_handoff` read in full (`operations/support_handoff.py`) to derive the default variant.
  - `StrictConfigModel` (extra=forbid, frozen) at `configuration/return_configuration.py:26`; `ReturnPlatformConfiguration` at :1639.
- Test result: n/a (no code yet).
- Next: step:02 config model + formatter allowlist + yaml block + one-line field append.

Noted grammar decision (recorded for RV): reproducing `compose_support_handoff`
output requires fixed banner/action lines that no `case_fact:`/`return_record:`/
`graph:` binding can produce. The config model therefore accepts a fourth,
purely declarative binding source `literal:<text>` (constant resolution, no
scripting, cannot gap). This is the minimal extension the brief's own
"default variant reproduces today's output" requirement implies; flagged here
rather than silently shipped.

## step:02 — template config grammar + formatter allowlist + production block (commit f2815d7)

- Files: `backend/src/return_platform/configuration/support_template_configuration.py` (new),
  `backend/src/return_platform/operations/template_formatters.py` (new, allowlist text|date|currency|address|item_list),
  `backend/src/return_platform/configuration/return_configuration.py` (integration duty: one import + one defaulted field append),
  `backend/config/returns/production.yaml` (`support_template:` block — default variant reproducing `compose_support_handoff`, parcel + LTL variant pair),
  `backend/tests/configuration/test_support_template_configuration.py` (23 tests).
- Commands: pytest tests/configuration + tests/operations/test_support_handoff.py → 219 passed.
- Notes: empty `variants` tuple is the defaulted pre-template release (renderer refuses loudly, caller falls back to compose path). Clause-less selector matches nothing; the default variant is reached only through `default_variant_id`. Case-level snapshot values (case id, workflow status at handoff, selected lines, verification verdicts, rendered policy line) are assembler-supplied `{"value": ...}` entries in the draft facts mapping with no factId — provenance distinguishes fact-log lines from draft-time snapshot lines.
- Test result: green. Next: renderer.

## step:03 — renderer + acceptance suite

- Files: `backend/src/return_platform/operations/support_template_renderer.py` (new),
  `backend/tests/operations/test_support_template_renderer.py` (21 tests).
- Public API: `async render_support_template(template: SupportTemplateConfiguration, draft: TemplateDraftInput, *, cache: TemplateRenderCache | None = None) -> RenderedTemplate{template_id, variant_id, subject, text, sections[RenderedSection{section_id, title, fields[RenderedField{field_id, label, value, source, source_path, fact_id, applied_fallback}], return_record_id}], gaps[TemplateGap{field_id, reason}]}`; `select_variant(template, context)`; graph seam `SupportTemplateGraphPort{read(path), synchronize(source, paths)}` — the production adapter (phase 2, activity-side) maps one `synchronize` call to exactly one `OnDemandSyncCoordinator.synchronize` (anchor verified; building `LogicalTargetedReadPlan`/`ActiveSchema` is activity wiring, out of a pure renderer).
- Covered: selector matching incl. parcel-vs-LTL and mixed→default; per-source binding + provenance; fallback-after-failure; required→TemplateGap; formatter refusal = binding failure; per-record section groups stamped `return_record_id` with record-partition fact isolation; batched sync (3 missing across 2 sources → exactly 2 synchronize calls); still-missing-after-sync → gap; within-draft cache (re-render: 0 extra syncs, 0 extra reads); `{{`/`}}` escaping + no value re-interpolation; **default-variant text equivalence vs `compose_support_handoff`** (character-for-character on the representative straight-through fixture).
- Equivalence scope note: body text compared exactly; the *subject* is the template's own `{field_id}` interpolation (`Return {order_number} · {customer_name}`), not `_subject`'s conditional joining — the subject grammar is interpolation-only by contract §8, so the composed subject's conditionals are not expressible and were not claimed.
- Test result: 44 passed (config + renderer). Next: preview endpoint.

## step:04 — variant preview endpoint + OpenAPI regen

- Files: `backend/src/return_platform/api/template_preview.py` (new — `POST /api/v1/config/support-template/preview`, gate `RETURNS_SESSION_READ` via `require_capability`; body = draft template + operator-chosen selector context; renders against a built-in fabricated sample case, no case id accepted, no graph port so previews never spend syncs), `backend/src/return_platform/main.py` (router import + mount — the one main.py touch; recorded as required for the endpoint and for `contracts:generate`, which exports OpenAPI off the mounted app), `backend/tests/api/test_template_preview_route.py` (7 tests: 200 render, context-driven variant selection, invalid draft 422, empty draft 422, gap surfaces, 403 capability, 401 unauthenticated), `frontend/openapi/return-platform.openapi.json` + `frontend/src/api/generated/return-platform.d.ts` (regenerated via `npm run contracts:generate` with `RETURN_PLATFORM_PYTHON` pointed at the main checkout venv).
- Test result: tests/api 296 passed. Next: config UI.

## step:05 — template config UI + variant preview

Resumed cold after the previous session hit an API session limit. Repo verified
against the ledger before writing: `git log` showed 096afd4 / f2815d7 / ea7e1cb
/ 176f1d5 off S1 head 7438e07, working tree clean. Anchors re-verified:
`DocumentEditor` at `AgentsSection.tsx:205/215` with the form|split|json modes
and the `governance.proposal.write`-gated `agentConfigApi.save` path;
`CONFIG_SECTIONS` + `sections(...)` icon map in `domains/registry.ts:162/235`
driving `ConfigurationPage.tsx`'s `TabBody`; `AgentsSection.a11y.test.tsx`
asserting the label *relationship* rather than a control count. No mismatch.

- Files (frontend):
  - `src/domains/config/DocumentEditor.tsx` (new) — the editor lifted out of
    `AgentsSection` unchanged in behaviour, parameterised on the things that
    differ between subjects (kicker, subtitle, badges, JSON `aria-label`, submit
    copy, read-only notice, not-an-object refusal) and on `onSubmit` —
    the *write path* — plus `renderResult` and a `footer(document)` slot that
    hands the caller the draft as it currently stands (`null` while raw JSON
    does not parse).
  - `src/domains/config/AgentsSection.tsx` — now the agent-specific half:
    registry list + the `PUT`-becomes-a-proposal write path + the proposal
    banner. Its 32 existing tests pass untouched, which is the evidence the
    extraction was behaviour-preserving.
  - `src/domains/config/SupportTemplateSection.tsx` (new) — the tab.
  - `src/api/supportTemplate.ts` (new) — `preview()` typed from the generated
    schemas; the draft itself is typed loosely and the module says why (a
    preview must be able to submit an *invalid* template, which is exactly what
    `SupportTemplateConfiguration` forbids).
  - `src/api/releasePublish.ts` + `src/components/PublishProgress.tsx` (new) —
    `runPublishPipeline` / `defaultReleaseId` / `PublishProgress` moved out of
    `AiControlCenterPage.tsx` (which now imports them) rather than copied: a
    third caller of a four-step publish is how two screens come to publish
    differently.
  - `src/domains/registry.ts` — `"Support Template"` in `CONFIG_SECTIONS` with a
    `Mail` icon; `ConfigurationPage.tsx` — one `case` arm.
  - `src/mocks/handlers/canonicalHandlers.ts` — preview handler (selects the
    variant from the posted context, so `dev:mock` exercises selection rather
    than always answering `default`) + `support_template` and `head_revision` on
    the runtime snapshot; `canonicalHandlers.contract.test.ts` — the route entry,
    so the body is validated against the committed OpenAPI.
  - `src/api/noVersionedPaths.test.ts` — third allowlist entry, with the reason
    written out (see deviations).
  - Tests: `SupportTemplateSection.test.tsx` (12), `.a11y.test.tsx` (6).
- Files (backend): `api/template_preview.py` + `tests/api/test_template_preview_route.py`
  — the preview now answers inside `APIResponse`; see deviations. All four
  committed OpenAPI snapshots regenerated.
- Write path (recorded decision): the brief says "reuse the DocumentEditor and
  its governance-proposal write path". The **editor** is reused literally. The
  **write path** is not, and cannot be: `agentConfigApi.save` is
  `PUT /api/agents/{manifest_id}`, which resolves a manifest id, refuses a
  non-agent module with `AGENT_NOT_FOUND`, and validates through the agent
  loader. `support_template` is a field on `ReturnPlatformConfiguration`, so it
  moves the way every other behaviour field moves: `POST /api/config/releases`
  (DRAFT) → `PATCH .../domains/RETURN_PLATFORM` with `{support_template: …}`
  (which validates the *result* against the full model) → promote VALIDATED →
  promote RELEASED with the snapshot's `head_revision` as the optimistic guard.
  That is the same governed lifecycle the AI provider and task editors already
  ride, and it is what makes the case's pinned `configurationReleaseId` a real
  template version pin (contracts.md §8). Flagged here rather than silently
  substituted.
- Deviations:
  1. **Preview response enveloped.** Step:04 shipped the route returning a bare
     model; the console's `apiClient` refuses any body without the platform
     `meta` envelope, so the endpoint was unreachable from the one screen it
     exists for. Changed to `APIResponse[SupportTemplatePreviewResponse]`, a
     test added for the envelope, OpenAPI regenerated. Own-module fix.
  2. **Three stale OpenAPI snapshots.** Step:04 regenerated only the frontend
     copy; `openapi/`, `backend/openapi/` and root `openapi.json` were left
     behind and `tests/test_openapi_contract_drift.py` was failing on all three
     *before* this step's changes. Regenerated with `scripts/export_openapi.py`
     (the `check_openapi_drift.py --write` the test names does not exist in the
     tree). Pre-existing failure closed, not introduced.
  3. **Versioned-path allowlist.** `noVersionedPaths.test.ts` forbids the shell
     calling `/api/v\d` paths. The preview route is `/api/v1/config/…` as
     shipped and as briefed. Added as a third recorded gap with the reason: the
     versionless `/api/config` surface keeps "promotion is its only mutation",
     and mounting a `POST` there would blur that invariant even though a preview
     writes nothing. Joins the same consolidation wave as the other two entries.
- Frontend outcome gates:
  - `modern-web-guidance:modern-web-guidance` — **available**, consulted
    (`accessibility`, `forms` guides): native elements over ARIA, `<label
    for>` on every preview control, programmatic names for generated fields,
    `:focus-visible` left to the existing token styles, `role="status"` for a
    result that must be announced without taking focus.
  - `design:design-system` — **available**, applied as reuse rather than
    invention: `premium-panel` / `premium-kicker` / `premium-field` and M3 role
    tokens only (`surface-container*`, `on-surface-variant`, `outline-variant`,
    `secondary-container`, `tertiary-container` for the fallback chip,
    `error-container` for gaps). No hex, no new token.
  - `design:design-critique`, `design:accessibility-review`, `design:ux-copy`,
    `design:design-handoff` — recorded in step:06 with their evidence.
- Test result: frontend `npm test` → 619 passed, 2 failed, both pre-existing in
  `src/domains/registry.test.ts` (`declares exactly the canonical domains`,
  `shares a visibility capability only where that is deliberate` — a
  `/shipments` domain the test predates; failing identically on the branch
  before this step, baseline 600 passed / 2 failed). `npm run lint` and
  `npm run typecheck` clean. Backend `pytest tests` → 4423 passed, 2 failed,
  both the known pre-existing ones (`test_a_rejected_return_still_opens_no_work_item`,
  `test_main_is_composition_only`). Zero introduced.
- Next: step:06 — outcome-gate evidence, handoff spec, delta report.

## step:06 — frontend outcome gates + handoff spec

- Files: `.plan/handoffs/V1-phase1.md` (new — the spec),
  `src/domains/config/DocumentEditor.tsx` (`notice` slot + `confirmSubmit`),
  `src/components/PublishProgress.tsx` (error/published/publishedNote optional),
  `src/domains/config/SupportTemplateSection.tsx` (findings 1, 3, 4 below),
  `src/domains/config/ConfigurationPage.test.tsx` (+1: the tab opens on its own
  deep link — registry entry, slug, and `TabBody` arm, end to end),
  `SupportTemplateSection.test.tsx` (+1: declining the publish confirmation
  calls nothing).
- Skill availability:
  - `modern-web-guidance:modern-web-guidance` — **available**; `accessibility`
    and `forms` guides retrieved and applied.
  - `design:design-system` — **available**; used as a reuse constraint (see
    the token table in the handoff: no new token, no hex, no arbitrary value).
  - `design:design-critique` — **available**; framework applied to the built
    screen. Findings acted on below.
  - `design:accessibility-review` — **available**; WCAG 2.1 AA pass, findings
    acted on below, assertions in `SupportTemplateSection.a11y.test.tsx`.
  - `design:ux-copy` — **available**; every state's copy rewritten to the
    what-happened / why / what-now shape. One recorded limitation: the publish
    confirmation is `window.confirm`, whose buttons cannot be labelled with the
    action. That is the console's existing idiom for destructive confirmations;
    a shared action-labelled dialog is worth doing once for all of them rather
    than as a one-off here. Named in the handoff.
  - `design:design-handoff` — **available**; spec at `.plan/handoffs/V1-phase1.md`.
  - **Degradation (recorded):** the browser pane is not displayable in this
    session, so the critique and the a11y review are analytic plus jsdom
    assertions rather than screenshot- and screen-reader-based. `dev:mock` was
    started and the MSW fixtures now carry a `support_template` and a
    `head_revision`, so the visual pass is reproducible by anyone with a
    display: `frontend-mock` → `/config/support-template`. Manual AT testing
    (VoiceOver/NVDA) remains genuinely unperformed and is not claimed.
  - `engineering:code-review` — deferred to the RV gate, which reviews the
    combined diff; this slice's ledger is the input to it.
- Critique / review findings acted on:
  1. Publish progress rendered *below* the editor while the button that starts
     it is in the header — the steps could be off-screen during the run. Moved
     into the editor's message stack via the new `notice` slot.
  2. `PublishProgress` forced a caller that reports its own errors and successes
     to pass `null`, `false`, `""`. Those three props are optional now.
  3. The live region was created together with its content, which is the one
     arrangement a screen reader is not obliged to announce. One `role="status"`
     line now exists from first render and carries the summary only — the whole
     rendered handoff in a live region would read the message aloud each time.
  4. Publishing changes what the platform runs and had no confirmation step.
     Added, worded as the consequence rather than as "are you sure".
- Test result: frontend `npm test` → 621 passed, 2 failed of 623 (the same two
  pre-existing `registry.test.ts` failures; baseline was 600/2). `npm run lint`
  and `npm run typecheck` clean. `npm run contracts:check` green.
  Backend `pytest tests` → 4423 passed, 2 failed, both known pre-existing.
- Next: none. Phase 1 is complete; items 3–4 and 6–8 are phase 2.

## Delta report — V1 phase 1

**Shipped (brief items 1, 2, 5).**

- *Item 1, template config.* `configuration/support_template_configuration.py`
  (StrictConfigModel grammar, model validators for default-variant existence,
  formatter-id membership and subject-placeholder resolution),
  `operations/template_formatters.py` (allowlist `text|date|currency|address|item_list`),
  the `support_template:` block in `production.yaml` reproducing today's
  `compose_support_handoff` output as the default variant plus a parcel/LTL
  pair, and the one-line field append on `ReturnPlatformConfiguration`.
- *Item 2, renderer.* `operations/support_template_renderer.py` — variant
  selection, per-source binding with provenance, fallback-after-failure,
  `TemplateGap`, per-record section groups, batched on-demand sync, within-draft
  cache, `{{`/`}}` escaping.
- *Item 5, config UI.* The Support Template tab, on the reused `DocumentEditor`,
  with the variant preview.

**DoD, phase-1 scope, each with its evidence.**

| Requirement | Where it is proved |
| --- | --- |
| Config validation (extra=forbid, unknown formatter, unresolvable subject placeholder) | `tests/configuration/test_support_template_configuration.py` (23) |
| Selector matching incl. parcel-vs-LTL, mixed → default | `tests/operations/test_support_template_renderer.py` |
| Per-source binding + fallback + gap | same |
| Batched sync (3 missing across 2 sources → 2 `synchronize` calls) | same |
| Brace escaping, no value re-interpolation | same |
| Within-draft cache (re-render: 0 extra syncs, 0 extra reads) | same |
| Default-variant equivalence with `compose_support_handoff` | same (character-for-character on the representative fixture; the *subject* is interpolation-only by contract §8 and the difference is recorded at step:03) |
| Config UI + preview | `SupportTemplateSection.test.tsx` (13), `.a11y.test.tsx` (6), `ConfigurationPage.test.tsx` (+1), MSW contract conformance for the preview route |
| `contracts:check` green | run after commit; clean |

**Not in phase 1, and untouched:** `return_case_workflow.py`,
`return_case_activities.py`, `workflows/worker.py`, every S2-owned module, the
review/panel API, and the panel UI. Brief items 3–4 and 6–8 are phase 2.

**Registered for whoever picks up phase 2 or the consolidation wave.**

1. `/api/v1/config/support-template/preview` is the third entry in
   `noVersionedPaths.test.ts`'s allowlist. It belongs with the other two in the
   wave that retires versioned prefixes.
2. The publish confirmation should become a shared action-labelled dialog
   rather than `window.confirm`, across every destructive confirmation on the
   console.
3. `registry.test.ts` has been failing on this branch and its parent: the
   registry declares a `/shipments` domain that the test's canonical list and
   its capability-collision list both predate. Not this slice's to fix, but it
   is red in every run and will keep looking like new breakage until somebody
   decides which of the two is right.

## step:07 — RV finding F4: `return_record:` reaches only what the projection declares

Contracts.md **AMENDMENT-2** (recorded by the orchestrator from RV's F4). Re-read
§1a and §8 before starting; **AMENDMENT-1** likewise settles F3 — `literal:` is
now the enumerated fourth binding source, so the shipped `production.yaml`
renders under the amended contract and no code change was needed for it.

- Files: `configuration/support_template_configuration.py`,
  `operations/support_template_renderer.py`,
  `tests/configuration/test_support_template_configuration.py` (+5),
  `tests/operations/test_support_template_renderer.py` (+1).
- The allowlist is `frozenset(ReturnRecordProjection.model_fields)` — **derived,
  never hand-listed**, so it cannot drift from the projection it describes. It
  is behind a cached function rather than a module constant because the
  projection package pulls the agent registry, which imports
  `return_configuration`, which imports this module: importing it at module
  level is a genuine cycle (`ImportError` on the first run), not a style
  question.
- Refused at **release validation**, so a template that reaches outside the
  projection cannot become a release. `return_record:__class__`,
  `return_record:active_shipments` (a real method, still not a field) and any
  undeclared name are all refused with the allowlist in the message.
- Two defensive changes so a bypass degrades instead of exploding:
  `_record_attribute` resolves only declared names, and the render loop turns a
  binding refusal into a field-level failure — a gap or a fallback — rather than
  an exception that takes the whole handoff down. `per_record`, which is asked
  while *planning* a render and before any field resolves, answers structurally
  instead of raising for the same reason. Tested: an undeclared attribute
  renders no `<class '...'>` into the message and produces a gap.
- Test result: 62 passed (config + renderer + preview route).
- Next: step:08 — F2, the binding vocabulary's single home.

## step:08 â€” RV findings F1 + F2: the default variant reproduces the handoff on every branch

F1 and F2 are one fix from two directions, so they are one step: F2 asks for a
single home for the binding vocabulary, and that home is exactly where F1's
conditional resolution has to live. Splitting them would have meant writing the
seam twice.

### What was wrong

RV rendered the shipped yaml against a case with no branch associate and no bay
recommendation and got five silent divergences with zero gaps. I reproduced that
across the whole matrix before changing anything, driving the **pre-fix** yaml
(from `7611506`) through the shipped renderer with the fact set an assembler
written against that yaml would have produced:

```
scenarios: 17   diverged: 11        (every one of them: gaps reported: 0)
```

Representative diffs, `compose_support_handoff` (-) vs the pre-fix default
variant (+):

```
=== no_branch_associate  (gaps reported: 0) ===
-- Branch Associate: Not recorded -- customer contact below
-- Customer Phone: 555-0199
-- Customer Email: buyer@example.com
+- Branch Associate: Not available
+- Branch Associate Email: Not available
+- Branch Associate Phone: Not available

=== bay_unresolved_with_a_reason  (gaps reported: 0) ===
-- Unresolved Reason: No bay free
-- Resolve manual bay assignment: no bay could be recommended.

=== case_state_unreadable  (gaps reported: 0) ===
-- Awaiting From Support: UNKNOWN -- case state could not be read

=== additional_required_details  (gaps reported: 0) ===
-- Original Packaging: NO
-- Warranty Claim: YES

=== associate_notes_impersonating_the_framing  (gaps reported: 0) ===
-[removed]
+BAY ASSIGNMENT:
```

The last one is a **sixth divergence RV did not list, and the worst of them**.
`compose_support_handoff` neutralises associate-typed text â€” a note containing a
section header would otherwise restructure the message for whoever reads it next
â€” and the template bound the raw `associate_notes` fact, so the neutralisation
was silently dropped. The template now binds the *rendered* notes.

### The fix

- **New `operations/support_template_draft.py`** â€” the seam, and the vocabulary's
  one home. It declares `SNAPSHOT_KEYS` (names the draft derives, no `factId`),
  `FACT_LOG_KEYS` (names an activity already writes), their union, and the
  conditional wording as named constants. `support_template_snapshot(...)` takes
  **`compose_support_handoff`'s own argument list** â€” deliberately, so a caller
  that can compose one can produce the other without deciding anything new, and
  so the equivalence matrix can drive both paths from one set of inputs.
- **The conditionals resolve to presence, not to a fallback.** The Â§8 clause
  grammar describes the shape of a *case*, never the presence of a value, so no
  `visibility_rule` can express "only when the bay was not recommended". Those
  lines are bound with **no `fallback`**: the producer omits the key and the
  renderer omits the line, exactly as the composed path omits it. A fallback
  there is what printed "Not available" where the truth was "no bay could be
  recommended, resolve manually".
- **The contact block is two mutually exclusive arms**, six fields, three ever
  present. Labels stay in configuration (the alternative â€” one pre-rendered
  block â€” would have taken the wording out of the operator's hands); it is the
  *arm* that is chosen in code, because the arm is the conditional.
- **`return_details.additional`** renders through one key holding already-
  labelled lines, because the labels are the release's and no template can know
  them. That preserves `support_handoff.py:157-160`'s property: a release that
  adds a required detail needs neither module edited.
- **`production.yaml`**: the customer, return-details, bay, verification and
  requested-action sections gain their conditional fields; `associate_notes`
  rebinds to `associate_notes_rendered`; the block comment now states what is
  claimed and what is not.
- **F2, the second copy:** `api/template_preview.py` held a hand-written second
  copy of the whole 24-key vocabulary. Deleted. The sample is `SAMPLE_CASE` in
  the seam module, expressed as compose arguments, and its facts come from
  `draft_facts` â€” so the route's "the same text as `compose_support_handoff`"
  claim is now `test_the_preview_is_the_composed_text`, which posts the
  **shipped** default variant and compares.

### The tests

- `TestComposedEquivalenceMatrix` â€” **17 scenarios Ã— 2 assertions**: the text
  matches the composed text, and no scenario reports a gap. Both halves must
  hold at once, which is precisely what the old single-fixture test could not
  see: it was green *and* dropping lines. Scenarios cover absent associate,
  associate with no email, no contact at all, three unresolved-bay shapes,
  outstanding dimensions, unreadable case state (including with a stale
  outstanding list), `additional` present, framing-impersonating notes, no
  selected lines, nothing confirmed, two policy shapes, and an almost-empty case.
- `TestTheMatrixItself` â€” the matrix is not vacuous: every declared snapshot key
  is produced by some scenario, and every scenario actually flips something.
- `tests/operations/test_support_template_draft.py` (new, 15) â€” the yaml and the
  vocabulary are compared **in both directions**, the two halves may not
  overlap, and every `FACT_LOG_KEYS` member is checked against
  `return_case_activities.py`'s own source for `_stated(facts, "<name>")`. That
  last one is what makes "these facts exist" a test rather than a claim.
- After the fix: **17/17 scenarios match, 0 gaps, 0 divergences.**

### Ledger correction (RV asked for it explicitly)

Step:03 recorded "body text compared exactly". That sentence was true of the one
fixture and false of the claim it appeared to make, and it pointed away from
this problem. **Corrected:** the body is now compared exactly across the
17-scenario branch matrix. The subject caveat recorded at step:03 stands
unchanged and is still the honest one â€” `_subject` joins its parts conditionally
and shortens them, and Â§8's subject grammar is interpolation-only, so the
template's subject is a different line by contract. The yaml comment says so
too, where an operator reading the template will see it.

- Test result: backend `pytest tests` â†’ **4494 passed**, 2 failed, both the known
  pre-existing ones (`test_a_rejected_return_still_opens_no_work_item`,
  `test_main_is_composition_only`). Ruff clean on every file this step touched.
- Next: step:09 â€” advisories A1 and A2.
