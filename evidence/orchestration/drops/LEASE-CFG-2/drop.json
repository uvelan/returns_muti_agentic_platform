{
  "lease_id": "LEASE-CFG-2",
  "branch": "feat/cfg-2-config-split",
  "base_sha": "73c276d231cd9e609678f8d29517d67cfb5d77a9",
  "head_sha": "PENDING",
  "files": [
    "backend/src/return_platform/configuration/settings.py (validator moved; DEFAULT_RETURN_CONFIGURATION_PATH/DEFAULT_AI_GATEWAY_CONFIGURATION_PATH repointed at the directories)",
    "backend/src/return_platform/configuration/composition.py (new; the composer)",
    "backend/src/return_platform/configuration/return_configuration.py (load_return_configuration: directory support)",
    "backend/src/return_platform/ai/routing/tasks.py (load_ai_gateway_configuration: directory support)",
    "backend/config/returns/** (new split tree, 8 parts + index, production.yaml untouched beside it)",
    "backend/config/ai_gateway/** (new split tree, index + 25 task files, anchors materialized)",
    "backend/tests/data/pre_split/** (new; frozen pre-split bytes)",
    "backend/tests/configuration/test_packaged_configuration_composition.py (new; equivalence + error tests)",
    "backend/tests/configuration/test_settings_configuration_paths.py (new)",
    "backend/tests/harness/configuration_tree.py (new; copied_configuration_tree)",
    "compose.yaml, backend/Dockerfile, .env.example (repointed at the directories)",
    "the seven raw-text test readers named in the brief, repointed at the part file that now holds their section",
    "53 additional test files (found, not in the brief's Owns list -- see ledger step:04) that hardcode 'production.yaml'/'ai_gateway.yaml' in a Path passed to a loader; mechanically repointed at the directory",
    "scripts/validate_stage4l_production.py, scripts/validate_stage4n_ai_gateway.py (take the directory)",
    "backend/config/README.md, backend/src/return_platform/configuration/README.md, backend/src/return_platform/ai/README.md, backend/config/workflows/return_session.yaml (docs)",
    "docs/evidence/stage4n_ai_gateway/validation_summary.json (byproduct of re-running validate_stage4n_ai_gateway.py; PASSED, 9 checks, timestamps only)"
  ],
  "tests": {
    "backend/tests/configuration/test_packaged_configuration_composition.py + test_settings_configuration_paths.py": "19 passed",
    "backend/tests/platform layering/cross-import/ai-lane-boundary": "6 passed",
    "backend/tests/{acceptance,configuration,operations,policy} repointed files": "79 passed",
    "pytest --collect-only after the 53-file bulk fix": "5347/5862 collected (515 deselected), zero collection errors",
    "full backend suite (tests -q --ignore=test_concurrent_activation.py), run before the 53-file bulk fix": "42 failed, 5295 passed, 10 skipped, 515 deselected",
    "full backend suite, run after the 53-file bulk fix and prose cleanup": "42 failed, 5295 passed, 10 skipped, 515 deselected -- identical counts; the same 9 modules RV's CFG-1 review named as pre-existing and base-identical; 5295 vs CFG-1's final 5276 = +19, exactly the tests this lease adds",
    "python scripts/check_openapi_drift.py": "status PASS, diffs: []",
    "scripts/validate_stage4n_ai_gateway.py end to end": "checksPassed=9, status=PASSED, against the split ai_gateway/ tree",
    "scripts/validate_stage4l_production.py": "fails at an unrelated pre-existing broken import (ReturnAgentRegistry does not exist), predates this lease -- confirmed via git log; the one line this lease owns was still fixed",
    "ruff check/format on owned src and all touched test files": "clean",
    "mypy on owned src": "47 pre-existing errors in 6 unowned files, confirmed present on the unmodified base with a fresh cache -- not introduced by this lease"
  },
  "remaining": [
    "deletion commit (git rm production.yaml, ai_gateway.yaml) -- last, once the full suite result above is confirmed",
    "full acceptance run pasted in the ledger after deletion: pytest, ruff, mypy, check_openapi_drift.py",
    "deliberate-failure check (duplicate 'workflow' declaration) pasted in the ledger",
    "final grep sweep confirming only the accepted exceptions remain"
  ],
  "merge_status": "PARTIAL"
}
