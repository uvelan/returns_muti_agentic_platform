module_id: agent.return_session_orchestrator
module_type: AGENT
schema_version: "1.0"
configuration_version: "2.0.0"
owner: RETURN_PLATFORM_ORCHESTRATION
status: DRAFT
dependencies:
  - module_id: workflow.return_session
    version_constraint: "^2.0"
payload:
  name: Return Session Orchestrator
  enabled: true
  execution_mode: DETERMINISTIC
  input_contexts: [SessionCommandContext]
  output_context: ReturnSessionContext
  direct_agent_calls_allowed: false
  idempotency_required: true

