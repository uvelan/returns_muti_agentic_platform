schema_version: "2.0"
release_id: returns-platform-v2-baseline
# The packaged baseline is what every process boots from, so it is a release and
# not a draft. It said DRAFT while the runtime loaded it regardless, which meant
# the one field that could have distinguished "finished" from "someone is still
# editing this" carried no weight anywhere.
status: ACTIVE
modules:
  agent.return_session_orchestrator:
    path: agents/return_session_orchestrator.yaml
  agent.order_discovery:
    path: agents/order_discovery.yaml
  agent.order_analysis:
    path: agents/order_analysis.yaml
  agent.return_workflow:
    path: agents/return_workflow.yaml
  agent.return_fulfillment:
    path: agents/return_fulfillment.yaml
  agent.bay_allocation:
    path: agents/bay_allocation.yaml
  agent.learning:
    path: agents/learning.yaml
  agent.graph_schema_design:
    path: agents/graph_schema_design.yaml
  policy.privacy:
    path: policies/privacy.yaml
  policy.clarification:
    path: policies/clarification.yaml
  policy.candidate_scoring:
    path: policies/candidate_scoring.yaml
  policy.return_eligibility:
    path: policies/return_eligibility.yaml
  workflow.return_session:
    path: workflows/return_session.yaml
  sync.order_partial:
    path: sync/order_partial.yaml
  sync.order_full:
    path: sync/order_full.yaml
  source.sales_inv:
    path: sources/sales_inv.yaml
  mapping.sales_inv_order:
    path: mappings/sales_inv_order.yaml
  graph.order_discovery:
    path: graph/order_discovery.yaml
  platform.system_store:
    path: platform/system_store.yaml
